# Backlog — Rock Climber: The Ritual

Source of truth. One table per domain section; a row joins exactly one section.

**Status** is one of: `proposed` · `approved` · `parked` · `rejected` · `shipped (vX.Y.Z)`
Shipped rows stay forever, as history. Rejected rows keep their reason.

**IDs** are `B<n>`, immutable, never reused. Take the next one by re-reading the highest number
in this file immediately before writing, never from memory.

**Notes** is where the value is: what is known now, what is proposed, the triage decision with
its date, and after shipping, what was actually built and how it was verified. Rows grow over
their lifetime; nothing is overwritten.

There is no priority column and no estimate column on purpose. Priority is the prose block below.

---

## Order of work, rewritten 2026-09-02 after Maor first played it on his iPhone

B1 is done, and it reordered everything, exactly as it was written to. Four of his five findings
were about things being unreachable or unexplained; none was about physics. So:

1. **B41** — the egg can only be unlocked by typing, and a phone has no keyboard. The whole
   web-zip, the glove and the ✦ panel — most of v0.1.0 and all of v0.2.0 — have never been seen
   by the person who asked for them. Nothing else on this list is worth as much.
2. **B40** — no way back to the menu. Every choice on the title screen is a one-way door.
3. **B42** — the four routes are four bare words.
4. **B27, B25** — now cheap to answer, because he is playing on the real device: the frame rate,
   and whether the death screen is too much on a phone.
5. Everything else, and B6 / B7 only once he has climbed it with the rope gone.

---

## Feel and controls

| ID | Item | Source | Status | Notes |
|----|------|--------|--------|-------|
| B1 | Maor plays the game end to end and reports what feels wrong | Claude 2026-09-02, self-review | shipped (v0.3.1) | Everything shipped so far was verified by screenshots, scripted harnesses and a bot. 67 tests and a bot that tops out prove it is not broken; they cannot prove it is good. Route length, grab feel, swing timing and the difficulty curve are all my judgement, unmeasured against a person. This row gates most of the open list. DONE 2026-09-02: Maor played the live v0.3.1 build on his own iPhone 15 and reported. Five things came back, and not one of them was a thing this backlog was worried about. The rope: he hates being saved and wants it gone (B43), which also explains the missing death animation he reported -- the plunge only exists after the rope is spent. The egg: the customisation window "doesn't exist", because ARACHNID can only be typed and a phone has no keyboard, so the entire web-zip has never been seen by the person who asked for it (B41). No way back to the menu (B40). The four route names mean nothing (B42). Nothing about grab feel, stamina, hold types or the difficulty curve -- the things every prior row spent its judgement on. The lesson is in the ratio: four of five findings are reachability and framing, none is physics. |
| B2 | Hand felt loose and magnetic when moving | Maor 2026-09-02 | shipped (v0.1.0) | Two causes. The hand spring was 16 rad/s and under-damped, so it overshot where you aimed; now 11.5 and critically damped, about 0.20 s a move. The magnetism was the grab snap closing in 0.15 s, which read as being sucked in; now 0.3 s, so the fingers arrive. There was no positional magnet in the code, which is why this needed Maor's description to find. |
| B3 | Gameplay became a fight after the grab-radius tightening | Maor 2026-09-02, "the gameplay is a nightmare" | shipped (v0.1.0) | Measured the cause rather than guessing: the grab target had halved to 8.3 cm, 11.5% of the reach circle, at the same moment the hand slowed to 0.27 s a move. Retuned to a 9.2 cm crimp floor, a 2.8 cm fingertip allowance and 0.20 s hands; smallest target is now 16.7% of reach and a jug is still 1.6x a crimp. |
| B4 | Guard against the game silently becoming unplayable again | Claude 2026-09-02, after B3 | shipped (v0.1.0) | `test/playability.test.js`. Fails if any hold drops under 15% of the reach circle, if a hand takes over 0.30 s to move, if one-arm hang time falls under 5 s on any hold type, or if a steady bot cannot top out. The bot drives the real sim: 128 grabs, 0 misses, 0 slips, 0 falls, summit. B3 happened with every unit test green, which is why this exists. CORRECTION 2026-09-02, after B46: the numbers above describe a bot that no longer exists. Walking holds in id order was the climb on a single line; on a field an id is only height order. The guard is now two independent things -- a pure graph check that start, every rune and the altar are connected under the reach rule and the alternating hands (all four roster seeds, and seeds 1-40 with the decoys taken out), and a bot that path-finds over that same graph and drives the real sim through B51's two primitives. Its numbers on seed 7 are 160 grabs, 5 misses, 0 crumbles, 0 falls, 63 s. Every threshold this row lists is unchanged, and two were added: no decoy taken, and under 5% of moves given up on. |
| B5 | You could grab past the rock onto bare wall | Maor 2026-09-02 | shipped (v0.1.0) | The radius was floored at a flat 16 cm, which on a small hold reached beyond the visible rock. It is now the hold's own size plus 2.8 cm of fingertip, so a crimp is a small target and a jug is a big one. |
| B6 | Chalk as a resource rather than decoration | Claude 2026-09-02 | proposed | The hands are visibly chalked and it means nothing. Chalking a free hand could buy back grip, which would give the resting hand a job and a reason to hang on a rune. Unscoped. |
| B7 | A second rope, or difficulty modes | Claude 2026-09-02 | proposed | One rope save is harsh on a first climb and trivial once you can climb. A choice at the title screen would serve both. Waiting on B1: this may be a non-issue. Superseded in part by B43 2026-09-02: Maor wants the one rope removed, so a second rope is dead. What is left of this row is difficulty modes, which B43's answer about what a fall costs will largely settle. |
| B45 | A hand snaps back to its rest position the moment you stop dragging | Maor 2026-09-02, first play on an iPhone 15 | shipped (v0.6.0) | "I don't like that after you drag the hand to move it and you stop dragging it comes back to where it was -- goes right back to where it was before I started dragging." This is deliberate and written into CONTRACTS: a free hand springs toward `shoulder + stick x REACH`, and a released stick returns to centre, so the hand drifts back to a rest offset. It was designed as a self-centring stick; he reads it as the game undoing his move. The alternative is that the stick steers a hand that STAYS where it is left, so a reach is a thing you do once rather than a thing you hold. That is a real change to how the sticks feel and it touches the hand spring, which is frozen against his own earlier feedback (B2, B3) -- so it wants his explicit decision on which of the two it should be, not a quiet retune. Note it also interacts with the grab: today you hold the stick and tap GRIP; if the hand stays put you could steer, let go and then tap. Shipped v0.6.0. Maor's second option was taken: the stick steers a hand that STAYS where it is left. WHAT WAS BUILT. `updateHand` no longer decays a free hand's stick vector after the thumb comes off; `hand._stick` IS the parked target, kept as an offset from the shoulder in REACH units, so a parked hand rides along when the body moves instead of hanging at a fixed point in the world. Nothing about the spring changed -- HAND_OMEGA 11.5, HAND_ZETA 1.0, REACH, SNAP and the grab radii are all untouched, so B2 and B3 stand -- the change is WHAT the target is, not how fast the hand gets there. The desktop keyboard already behaved this way (its virtual stick holds its value when the keys come up), so this makes touch match the keyboard rather than inventing a third behaviour. `Input.L.active` / `Input.R.active` were added, because a stick reading zero is not a stick nobody is touching: input.js sets `active` while a finger is on that ring, while the mouse is driving that hand, while a movement key is down, and for exactly one read after a recenter (Escape or a grip key, which is a command and not a release), and the sim reads an active stick at the ring centre as a deliberate steer back to rest. An Input without the flag keeps the pre-B45 rule that only a non-zero vector steers, which is the safe fallback because ignoring a zero stick can only ever leave a hand where it is -- the debug autopilot in main.js and the test bots all run on that path, unchanged. Removed as dead: CFG.LINGER, CFG.DRIFT_TAU and `hand._linger`. That was measured before deleting: the linger never did anything after a grab release (the hand had already left the hold by 13 mm at 50 ms) because the grab clears `_stick`; it only ever delayed the drift that B45 removes. THE RULE FOR EACH STATE. Climb start: the sticks read zero and are not active, both hands stay on their holds, nothing moves. Steer then release: the hand holds its last steered target for as long as you leave it, riding the shoulder. After a grab release: the grab cleared `_stick`, so the arm relaxes to the rest offset once -- kept deliberately, because letting go of rock is your own move rather than the stick undoing one, and a hand parked inside the grab zone of the hold it just released would have to be steered off it before it could aim, and would re-take that hold once the 6 s skip lapsed. Falling and landing grounded: nothing resets the park, because moving a hand at the start of a fall would sabotage the grace-window save; the hands keep their targets and ride with the body as it walks back under the route. After a swing: the left stick pumps the swing and steers the left hand, so the arm parks where the last pump left it, and a web attach releases both hands, which puts them at the rest offset. VERIFIED WITH NUMBERS. Steer a free hand, let go of the stick, run 2 s: it moves 0.0 cm and sits 48.2 cm from the rest offset. The same run on the old sim moved it 47.9 cm, straight back to rest. Riding the body: grounded at the base the climber walks 54 cm back under the route and the parked hand travels 70 cm with him, holding its shoulder offset to 0.2 mm (old sim: 405 mm of error, because the hand went home instead). Mid-swing the parked target holds its offset to 0.00 mm across a 2.5 m body swing (old sim: 774 mm). TESTS 70 to 78, nothing weakened. New in sim.test.js: a released stick leaves the hand where it was steered, a hand that has not been steered since it left the rock hangs at rest, a parked hand rides with the body while the other hand climbs, a parked hand walks back under the route with you while grounded, a zero stick at climb start does not move a hand off its hold, and steer then let go then tap GRIP still misses, arms and closes on the hold. New in input.test.js: `active` for a touch down and up and for keys held and released, plus the mouse and LOOK cases. New in web.test.js: the swing pump parks the hand. The playability bot is untouched by the change, as expected, since it steers every frame and taps while steering: its runs are identical before and after, all four roster seeds topping out with zero falls -- seed 7 Ritual 128 grabs 0 misses 0 falls in 36.0 s, seed 21 Ladder 125 grabs 35.9 s, seed 4 Serpent 128 grabs 36.4 s, seed 19 Ordeal 124 grabs 36.3 s. NOT VERIFIED: no thumb has touched it. This is a sim and test result; whether the parked hand feels right on the phone is Maor's call. Release now comes from the stick rather than a button (B51): pushing the stick belonging to a gripping hand past CFG.RELEASE_DEADZONE is what lets go, and the same push steers the hand it just freed, so parking and reaching are one gesture. |
| B54 | Unlimited stamina while the controls are being reworked (testing default) | Maor 2026-09-02, "for this state of development make the stamina unlimited, so we can test things out" | shipped (v0.6.0) | Direct instruction, and a temporary one, not a redesign: the stamina mechanic itself (B2/B3/B8, tuned against Maor's own feedback) is untouched and stays switchable back with one change. A per-climb flag, `state.unlimitedStamina`, not a CFG constant — CFG stays frozen and the existing stamina tests keep testing the real mechanic, untouched, with the flag false. `createClimber` defaults it false. `updateStamina()` in `src/sim.js` early-outs at the top when it is true: both hands pinned to `stamina = 1`, no drain (two-hand or one-hand), no forced release at zero, and no per-kind hold-quality hang timer — a sloper's `slip: 9.0` included, since that timer lives in the same loop the early-out skips. Refill logic is irrelevant when pinned. Decoys still crumble under a hovering hand regardless: that is rock giving way, not a stamina mechanic, and `crumble()` never reads stamina. `src/main.js` sets the flag from a query default: `const UNLIMITED_STAMINA = !query.has('stamina')` near the other query flags at the top of the file, so the default ships UNLIMITED and `?stamina=1` on a page turns the real mechanic back on for a feel test — one line, `state.unlimitedStamina = UNLIMITED_STAMINA;`, added right after each of the three `state.web.unlocked = spiderUnlocked();` sites (`start()`, `restart()`, `toTitle()`); a fourth site in the `onSkinChange` re-skin callback was left alone, since it is not one of those three functions and the flag survives a re-skin anyway. To ship the real mechanic instead of this testing default, flip `UNLIMITED_STAMINA`'s definition to a bare `false`. `src/hud.js` needed no change: it reads `hand.stamina` and `CFG.TREMBLE_AT` directly for the arcs, `.stick.gripping` / `.stick.slipping`, and the low-stamina vignette, all of which already read as full/gripping/absent once stamina is pinned at 1, and `updateHand()`'s tremble calculation (`clamp01((CFG.TREMBLE_AT - stamina) / CFG.TREMBLE_AT)`) is already 0 at `stamina === 1` with no code change. VERIFIED: `node --test test/` 116 to 119, nothing weakened — the pre-existing stamina/slip tests are untouched and still run with the flag false. Three new tests in `test/sim.test.js`: `createClimber` defaults `unlimitedStamina` to false; with the flag true, a one-arm hang on a crimp (`grip: 'crimp'`, the worst drain multiplier, 1.85x) for 60 s holds stamina at exactly 1 and fires no `slip` event, where the same 60 s without the flag would drain roughly nine times over and force a release well before 10 s; and with the flag true, hanging on a sloper (`slip: 9.0`) for 12 s — past its hang timer — never releases and fires no `slip` event. NOT VERIFIED: no thumb has touched a real climb with the flag either way; the HUD read is inferred from the code, not from a screenshot. |
| B51 | No GRIP buttons: a hovering hand grabs, a pushed stick lets go | Maor 2026-09-02, after playing v0.5.0, "remove the holding buttons - its mechanic is annoying, make it so that when the hand hover it will grab the rock if its being hover on" | shipped (v0.6.0) | His words, and the whole control model changed to match. THE RULES NOW. Grabbing is automatic: a free hand that stays inside grabRadius(hold) for CFG.HOVER_GRAB_DWELL (0.12 s, a feel constant he may want retuned) closes on it. The grab radius itself did not move -- B3 and B5 set it against his own feedback -- so the target you aim at is the one you already had, and the dwell is the only thing between reaching for a hold and sweeping past one. Runes, the summit and decoys are taken exactly the same way and keep their events, so a decoy still crumbles under a hovering hand (B10). Letting go is a stick push: while a hand grips, pushing ITS OWN stick past CFG.RELEASE_DEADZONE (0.35 of full deflection, a feel constant) opens the fingers, and that same push is already steering the hand where it is going, so a move is one gesture instead of tap-steer-tap. Under the deadzone a gripping hand does not move at all, so a thumb resting on the ring cannot drop you, and one stick only ever touches its own hand, so no single thumb can let go of the cliff. It is the PUSH and not the pushed stick: the stick has to come back inside the deadzone before it can let go again, or the stick that steered a hand onto rock would drop it the frame after it closed, for ever. On a touch ring the thumb lifting off does that by itself; on the keyboard Escape centres both virtual sticks. GUARDS. The hold a hand comes off is locked out of that hand until the hand is CFG.SKIP_CLEAR (4 cm) outside its grab radius -- a distance, never a timer, because with the grab automatic a hand parked inside that rock would otherwise be taken back by it with no input at all the moment a timer lapsed (measured on the B45 branch: released at 3.6 s, grabbed again at 5.18 s, untouched). CFG.SKIP_TIME (6 s) and CFG.ARM_TIME are gone; CFG.REGRIP_LOCK (0.14 s) still holds the fingers open for a beat after a release. Falling lifts both, which is what makes the grace window mean anything now: CFG.GRACE is unchanged at 0.25 s, and a hand still on the rock when the last hand goes re-closes on it at 0.12 s -- the panic re-grab. The exception is a hold that spat the hand off in a slip: fingers that failed there do not re-close on it, or a spent hand would grab and slip on the same rock for ever. (BOTH of those sentences are superseded by the review pass at the end of this row: falling lifts the regrip beat only, never the lock on the hold you just let go of.) The right hand does not auto-grab while the web line is out, so a hand that is aiming or has just shot cannot snag a hold and cancel the shot; the free left hand catching rock mid-swing still ends the swing, as before. The hover CUE (Hand.nearId, nearDist and hover, which world.js draws the glow from) now reports the rock the hand is actually over even when that rock is the locked-out one, so the highlight can no longer point at a different hold than the hand is sitting on. REMOVED. #grip-l and #grip-r and their CSS, hud.grips, setPill and pillState and PILL_LABEL, the keyboard grip keys Q and Enter and Slash, the mouse-button grips (the right button is now only the web-zip hold-to-aim; the left does nothing), Input.tapL and Input.tapR and Input.holdL, the sim tap() and the arm event, and the one-read recenter pulse in input.js -- it was droppable by design, since any display frame that ran no sim step ate it, and with the grip keys gone Escape is a plain stick recentre that claims nothing. Hand.armed stays on the hand as a constant false because world.js reads it for the hover ring; nothing sets it. WHERE THE HUD STATE WENT. Onto the two things you actually touch. The stick ring carries its own hand: .stick.gripping lights the knob gold, .stick.slipping turns it red and pulses, .stick.miss is the old pill shake, and the stamina arc around the same ring is unchanged. The WEB pad carries the whole web-zip: can for ready, new aim and out classes, and cool with the same --cd drain that used to run across the right pill. Title card copy changed only where it described grabbing. THE BOT. Both test files drive the sim through the same two primitives a thumb has, named at the top of each file so that B46 path-finding can call them: steerToHold(state, side, hold) and releaseHand(state, side, dir), which centres the stick for a frame and then pushes it full toward dir. botGrab is now one gesture: push the stick at the next hold (that is the release), then keep steering until the hand comes to rest on rock and closes by itself. The miss metric survives and got stronger: a miss is a hand that entered a hold and left before the dwell closed, which is exactly what the HUD shakes on. VERIFIED WITH NUMBERS. 82 tests pass at the time of writing, up from 79, nothing weakened (116 after the merges and the review pass below). The playability bot tops out every roster seed with zero falls and zero slips: seed 7 Ritual 128 grabs 3 misses (2%) 45.5 s, seed 21 Ladder 125 grabs 3 misses 45.3 s, seed 4 Serpent 128 grabs 3 misses 46.0 s, seed 19 Ordeal 124 grabs 4 misses 45.7 s -- all inside the 30 to 420 s window and far under the 35% miss ceiling. The climb is 9 s slower than under the tap (36 s), which is the 128 dwells it now pays. New tests: the fingers close after the dwell and not a frame before; a hand sweeping across a hold (0.100 s inside the radius, under the 0.12 s dwell) does not snag it and reports the miss; a stick held just under the deadzone for 3 s never lets go; a push past it lets go and the hand follows the stick out to the rim; one stick buried for 3 s only ever drops its own hand; the hold just released is not taken back while the hand sits on it (10 s of no input, no grab) and is taken again after a deliberate trip off the rock and back (under 2 s); a decoy is grabbed by hovering and crumbles once; and the grace window still saves a fall, at 0.12 s of its 0.25 s. NOT VERIFIED. No thumb has touched it. Two things a player could hate, for him to judge on the phone: a careful adjustment of a gripping hand is impossible, since any stick move big enough to register is a release, so the 0.35 deadzone is the whole margin a resting thumb has; and mid-swing the free hand catches any rock it comes to rest on, which can end a swing you wanted to keep. Both are one constant away from a retune. MID-SWING IT TAKES A REACH (manager decision after B46 measured it on a field of ~1900 holds; the owner can overrule it). Hanging on the web both hands are free and ride the body across the whole face, so with the grab automatic a parked hand was inside some hold within a few frames and the swing ended almost as soon as it began -- on a densely bolted wall, instantly, which kills the web-zip. So while phase is swinging a hand only closes on rock its OWN stick is actively pushing it into (past CFG.RELEASE_DEADZONE, the same deflection that lets go), with the dwell still on top: you reach out to catch rock, and flying past it catches nothing. Everywhere else -- climbing, standing at the base, and the falling grace window -- hovering grabs exactly as above. Two tests hold it: a swing past a jug placed at the turn of the pendulum, sticks idle, does not grab through 5 s of hand-on-rock, and the same swing with the stick pushed at the hold catches it and ends the swing. (Refined in the review pass below: a push in roughly the right direction is not enough, because that is what a pump is.) The right hand also never auto-grabs while the line is out at all, so a hand that is aiming or has just shot cannot snag a hold and cancel the shot. REVIEW PASS on the merged tree (reviewer measured 336934b; main had moved to B47 and B50 under it). One blocker and four shoulds, all in. THE BLOCKER, and it was a real one: falling used to lift the lock on the hold a hand had just let go of, so a push on both sticks from a two-hand hang gave release, release, fall -- and then, 0.125 s later, both hands welded themselves back onto THE SAME TWO HOLDS (15 of 16 seed x magnitude cases; the frame trace reads release:L#72 release:R#73 fall, miss:L#72 grab:R#73 grab:L#74). That killed B43 from a two-hand stance, and it landed the climber on a hand with the release latch down and the thumb still buried, which is the exact stuck hand the latch exists to prevent, reached through the fall. The lock now holds in every phase; falling waives only the regrip beat. CFG.GRACE is untouched and still saves you -- on a hold you had not just left, which is what reaching for one is. Two tests: both sticks pushed with nothing else in reach falls to the ground with no grab event at all, and with a second hold in reach that hold catches it inside the window and the latch behaves on the way out. THE SWING RULE was too loose: an ordinary pump (the stick swept through the ring) counted as a reach, so pumping past a hold grabbed it, cut the line and spent a fall on a ~1.4 s cycle. The manager proposed a 45 degree arc; measured, that does not separate them -- a sine pump sits inside any given 45 degree arc for 0.61 s of every 1.4 s cycle, five dwells. What does separate them is where the stick PARKS the hand: mid-swing a hand now closes only on a hold the stick is sending it to, the resting place it picks (the same target updateHand springs to) inside that hold, for the whole dwell. A pump sends the hand to arm.s length, past everything. Tested with 12 s of pumping past a jug in the path: no grab, swing kept. MISS SPAM: a hand on a hold.s rim crossed in and out with a millimetre of sway and fired ~49 miss events a second, each one shaking the ring, playing a cue and kicking the camera. Two guards, both tested and both shown to fail without the fix: CFG.HOVER_HYST 0.012 (a dwell leaves by 12 mm more than it came in by, so an 8 mm drift keeps the hold and the fingers close as they should) and CFG.MISS_COOLDOWN 0.30 (two rims crossed 0.125 s apart in one flick report one miss, not two). SLIP PING-PONG: two holds whose radii overlap -- seed 7.s 126 and 128 overlap by 20 mm, and every roster route has such a pair -- gave a spent hand somewhere to go the instant it came off, so it slipped off A, closed on B, slipped, closed on A, for ever. A hand that slips now takes nothing until it has CFG.SLIP_REST 0.15 stamina back: a slip is the game telling you to shake out. NIT: CFG.RELEASE_CONFIRM 0.016 (two 120 Hz steps) -- the push has to hold past the deadzone to count, so 0.3501 for one frame is not a hand off the wall. The keyboard title card now says Escape re-centres the sticks, which is how the release latch is re-armed without a touch ring to lift a thumb off. CSS: the gold gripping ring moved onto its own layer, because the crit pulse animates the box-shadow it used to live in and a gripping hand on a spent arm stopped looking held; the miss shake and the crit pulse are now named together so one does not silence the other; both are in the reduced-motion list. TESTS 110 to 116 on the merged tree, none weakened. THE SWING RULE AGAIN, ON THE REAL FIELD (B46). The destination rule above was measured on the field-of-holds branch and it is not enough there: with the sticks idle a swing survives 12 s 24 times out of 24, but a PUMPED swing ended in a grab 24 of 24, between 0.55 and 10.97 s, median 4.45 s, and a constant-speed circle still caught 7 of 24. Two reasons, both measured. A sine sweep LINGERS about 0.3 s at each end of its stroke, which any short test reads as an aim; and at 59 to 67 per cent coverage there is rock wherever the arm points, so a moment of lingering is always a moment of lingering ON something. Thinning the field does not save it (23 of 24 on the thinned one). So a mid-swing catch is now a HELD reach: the stick must stay on ONE hold for CFG.CATCH_HOLD (0.5 s) unbroken -- past CFG.RELEASE_DEADZONE, sending the hand into that hold, and within CFG.CATCH_CONE (30 degrees) of that hold.s own direction from the shoulder -- and the ordinary hover dwell still has to run on top of it, with the hand actually on the rock. The hold changing, any of those lapsing, or the stick reversing (more than 90 degrees off its last direction, which is exactly what a pump does at each stroke end) starts the clock over. 0.5 s is the number because it is longer than any pump lingers and shorter than a deliberate reach takes anyway. AFTER, re-running the same 24-swing matrix (four roster seeds x six shot aims x three stick regimes, 12 s each): dense field idle 24/24 kept, pumped 24/24, circle 24/24; thinned field idle 24/24, pumped 24/24, circle 24/24 -- every swing survives under every regime, on both densities. A deliberate held reach still catches and still ends the swing, which is how you get off the line. B46.s test went from red to green as written, not loosened. TESTS 116 to 124 with B46.s merged in, all green (the suite takes about 72 s now: thinned-field generation is slow). |
| B43 | Remove the rope that saves you | Maor 2026-09-02, first play on an iPhone 15, "I hate the rope that saves you. I want you to remove it" | shipped (v0.4.0) | Direct instruction, not a proposal. Today `beginFall` sets `_fall.doomed = state.fallCount >= 1`, so the FIRST fall is always caught 1.3 m below where you slipped and the climb just carries on; only a second fall is a real drop. That is also why Maor reports "there is no death fall animation" in the same message: the plunge B24 built -- terminal velocity, the head pitching over, the lens widening 22 degrees, the red bloom, the end screen -- is unreachable until the rope is spent, and he never spent it. Removing the rope makes the drop the normal outcome and gives him the animation he says is missing, so this row closes that complaint too. Open question put to Maor: what a fall costs. Note `state.checkpoint` is written on every rune and read by nobody -- the runes have been called checkpoints since the design interview and have never once acted as one, which is the obvious thing to spend here. Supersedes B7 (a second rope, or difficulty modes): a second rope is dead. Touches `test/playability.test.js`, which asserts the rope catches. Shipped v0.4.0. Maor chose the pure rule from three options: nothing catches you, every fall runs the whole cliff. `beginFall` no longer computes `doomed`; the falling integrator always plunges at terminal velocity to the ground, ending in `phase 'fallen'` with `impact` and `fallen`. Gone with it: the `caught` phase, the `catch` event, `state.ropeAnchor`, `state.fallCount`, CFG's ROPE_SLACK / ROPE_OMEGA / ROPE_ZETA / ROPE_ANCHOR_UP, the harness cue in audio.js, the catch bounce in camera.js, and in world.js the climber's strand, the anchor ring and bolt at the summit and the belay line down the face -- a top-rope rig on the wall would have kept promising a save that no longer exists. The GRACE window stays: a quarter second after a slip in which a hand can still find rock. That is skill, not a net. ONE JUDGEMENT CALL, flagged for Maor to overrule: letting go with your feet still on the ground is not a fall. `_fall.from <= FLOOR + HANG_TWO` puts you in a new `grounded` phase where you stand at the base, walk back under the start holds and can take the rock again -- without it, releasing on the very first frame killed you from 3 cm, which reads as broken rather than hard. THE HUD followed: the FALLS readout counted rope catches and can now only ever read 0, so it is hidden, and the end screen's Falls stat became HIGHEST, the metres you reached, which is the number that means something when you die. The title card now says 'Nothing catches you: one fall is the whole cliff.' TESTS: still 70, and the guards got stronger rather than weaker. The playability bot now counts `fall` EVENTS instead of reading `fallCount`, which catches a slip the grace window recovers -- something the old counter never saw -- and it still tops out all four roster seeds with zero falls. The four rope tests became four fall tests: the plunge reaches the ground with the right events and respects terminal velocity, a grab inside the grace window still saves it, past the window nothing can be grabbed even when a hold goes by, and standing at the base is not a fall. The autopilot rope test became a fall from five heights along the route, each asserted to reach the ground with no catch. VERIFIED in the browser at 390x844: no rope object anywhere in the scene, no `ropeAnchor` or `fallCount` on the state, console clean; a fall from 17.7 m ran to the ground in 1.85 s at up to 18.1 m/s and raised the death screen reading HIGHEST 17.7 m; Climb again resets to y 0.78 with the runes dark; and letting go at the base leaves you standing at x 0.03, alive. |

## World and route

| ID | Item | Source | Status | Notes |
|----|------|--------|--------|-------|
| B8 | The rocks were undifferentiated, so there was no difficulty | Maor 2026-09-02 | shipped (v0.1.0) | Four hold types with their own stamina cost: jug, edge, crimp, sloper, and a sloper drops you after 9 s however fresh you are. Difficulty rises with height: 16% poor rock in the first 8 m, 38% in the last. Crimps are visibly smaller so the wall can be read before it is touched. A route test asserts the curve so it cannot silently flatten. CORRECTION 2026-09-02, after B46: '16% poor rock in the first 8 m, 38% in the last' was measured on the 125-hold line. On the field it is 14.8% low and 55.8% high. The guard was also too loose to do its job -- 'the top is 10 points poorer than the bottom' still passed with the curve flattened threefold. It now asserts a 28-point gap AND a ratio of 2.8, which the shipped curve clears at 40.9 points and 3.76, and which a 1.5x flattening (27.3 points) and a 3x flattening (15.0 points, ratio 2.50) both fail. |
| B9 | Blue overlay appearing on some rocks | Maor 2026-09-02 | shipped (v0.1.0) | It was the teal waymark glyphs, painted on the wall every 2.6 m beside the route. Turned off, like the hover ring before them. The wall is read, not labelled. |
| B10 | Decoy rocks that crumble | Maor 2026-09-02, "fake rocks to trick the players" | shipped (v0.1.0) | 8 per route, generated beside the line and never on it, deliberately never the only way up. Identical to real rock until weighed, then they crack, drop away and are gone for good. Sound and physics done; visual feedback is B11. CORRECTION 2026-09-02, after B46: 'generated beside the line and never on it' is no longer what happens, and the new way is better. A decoy is now a field hold LIFTED OUT of route.holds, so it sits exactly where a hold would sit and is worth trying -- and because route.holds is therefore already the field without the decoys, 'never the only way up' is something the connectivity check proves for every seed from 1 to 40, rather than something the placement rule hopes for. Still 8, still never a rune or the altar. On a thinned field the generator also re-walks the whole climb before it lifts each one out. |
| B11 | A decoy needs a dust burst when it goes | Claude 2026-09-02 | shipped (v0.3.0) | It crumbles, sounds right and falls, but there is no puff of dust and nothing in the HUD. Never yet seen in a real play session, so its readability is unverified. Shipped v0.3.0: 18 grit particles fire from the sim's `crumble` event, spawned in world.js out of a fixed 42-particle pool (one draw call, nothing allocated per burst, the whole loop skipped when nothing is alive) on the same `createParticles` system as the embers -- but non-additive and dimmed with the night, so it reads as dust and not as another glow. Thrown outward, biased down and out of the face, gravity 3.2 m/s2, drag e^-3.4t, life 0.55-1.1 s, alpha snapping on and thinning out. Driven by the event rather than by polling `broken`, so it lands on the frame the hand comes off. Verified in the browser by tapping a decoy: the crumble fired, 18 particles came alive at the decoy's own coordinates and were gone inside 1.6 s, console clean; photographed at 390x844. The 'nothing in the HUD' half of this note was out of scope and is untouched. |
| B12 | Route was ~210 grabs, too long for one sitting | Claude 2026-09-02 | shipped (v0.1.0) | The frozen reach and hang geometry caps a move at 0.228 m, so 40 m meant about 210 grabs. Shortened the route to 24 m with runes every 6 m: 125 holds. Tried raising the reach instead and reverted it, because a longer reach made a free hand steer toward the next hold rather than the one under it. CORRECTION 2026-09-02, after B46: '125 holds' is the old line. The field is about 1965 on seed 7, and the count is now a knob rather than a consequence -- see B46. What this row was really about, the LENGTH of the climb, is unchanged: the bot tops out in 160 grabs and 63 s of game time, against 128 grabs on the line. |
| B13 | More than one route, or a daily seed | Claude 2026-09-02 | shipped (v0.3.0) | Generation is already deterministic per seed; only seed 7 is ever used. Cheap to expose, but pointless before B1. Shipped v0.3.0: `?seed=` picks the line, and the title screen offers four hand-checked ones -- Ritual (7, the original), Ladder (21: 42 jugs, 5% poor rock down low against 40% up high), Serpent (4: wanders 0.70 m either side of centre), Ordeal (19: 31 crimps, 18 slopers, 68% poor rock up high, 9 decoys). Chosen by screening seeds 1-40 through the playability bot: all 40 topped out with 0 misses, so the four were picked for character rather than validity, and each was then walked in the browser. Choosing one reloads the page with the seed in the URL (keeping the other parameters), because a new route rebuilds the cliff, the decoys and the arms -- and it leaves a link that opens the same climb. An unlisted `?seed=` still generates under the same rules and shows as its own pill. Tests: the bot now climbs every seed on the roster, the roster is checked for duplicates and names, and the parameter parsing is unit-tested. 67 tests to 70. RE-DERIVED for v0.6.0 (B46), and the honest answer is that the four seeds barely differ any more. On the old single line a seed decided every hold, so seeds really were different climbs -- 42 jugs here, 31 crimps there. A field is ~1900 holds drawn from the same height-based distribution and the law of large numbers flattens it: measured across the roster the grip mix is within 2% (jugs 417-447, edges 824-851, crimps 390-408, slopers 202-238) and poor rock up high is 50-52% on all four, against 13-15% down low on all four. The one thing a seed still decides is WHERE THE THREE RUNES SIT, and so how far across the face a climb has to travel. Ritual (7): runes at -1.11, 1.17, -1.56, 7.7 m of traverse. Ladder (21): 1.03, -0.97, 1.28, 6.4 m. Serpent (4): 0.58, 1.30, -1.51, 5.7 m -- the LEAST travel, so its old name is now backwards and the note says what is true instead of keeping the story. Ordeal (19): -1.98, 1.13, -1.22, 8.5 m, the widest. The notes in SEEDS and the README now say only that. Giving the seeds real character back means varying the generator per seed -- density, spread, how fast the rock hardens -- which is a design call for Maor, not a description, so it was not invented here. CORRECTION 2026-09-02, after the B46 review: the traverse figures above were rounded from a build that has since changed. Re-measured on the shipped field: Ritual 7.65 m, Ladder 6.41 m, Serpent 5.71 m, Ordeal 8.48 m. Poor rock up high is 49.4-55.8% across the roster against 13.1-14.8% low, so the seeds are close but not identical in difficulty either. The notes in SEEDS and the README carry the corrected numbers. |
| B46 | The route is one straight narrow path; the rocks should spread across the cliff | Maor 2026-09-02, first play on an iPhone 15 | shipped (v0.6.0) | "Make it so the rocks will spread horizontally and with different routes, since right now it's like a straight weird path which I don't like." The generator walks a single line up the wall: ROUTE in route.js keeps every hold within reach of the projected shoulder, which by construction produces a narrow ladder. The cliff is 9 m wide and the climb uses about 1.4 m of it. B13's four seeds vary the character of that line (Serpent wanders 0.70 m either side) but they are all still one line. Wanted: holds spread across the face, and more than one way up, so the wall reads as rock rather than as a staircase. The constraint to respect is the one B12 and the playability test encode -- every hold reachable at <= 0.9 REACH from the projected shoulder, and a steady bot must still top out -- so branching means real alternative lines, each of which has to pass the bot, not just scattered decoration. Shipped v0.6.0 as A FIELD OF HOLDS -- Maor's own choice of the options put to him. GENERATOR (src/route.js, rewritten): a staggered lattice grown row by row over x in [-3.6, 3.6] from the two start holds at y 1.2 to the altar at 24. CONSTANTS, all named in ROUTE: SPREAD 3.6 (half-width of the field, against the old line's 0.7), ROW_DY 0.165 and COL_DX 0.72 with every other row offset by half a column -- that offset is what gives each hold an up-left AND an up-right neighbour -- DRIFT 0.02 of sideways slide per row so the field flows rather than rules itself into a pegboard, per-hold jitter JITTER_X 0.03 and JITTER_Y 0.008, THIN 0.22 of sites left as bare rock, MIN_SEP 0.28 between scattered holds and MIN_FIT 0.245 for one squeezed in by the repair, RUNE_SEP 0.34 around a rest, CLEAR 0.03 of rock between two rims, SIZE 0.092 to 0.148, PLACE_USE 0.895 against the contract's REACH_USE 0.9 so every link the generator builds has 3 mm of slack. About 1900 holds a route (1859 to 1936 across the roster), roughly 15x the old line's 125. WHY THAT MANY, and it is not a choice: the frozen reach geometry caps a move at 0.228 m and only straight above the free shoulder, so rows have to sit ~0.165 m apart, and no two holds may be closer than ~0.28 m. That fixes the density of a climbable face at about one hold per 0.09 sq m. Covering 7.2 m by 22.8 m at that density is ~1600 holds before any repair. A sparser field is not a sparser field, it is a broken one. THE HARD PART was not the scatter, it was that hands alternate. A hold whose only way up is a left-hand move stops a climber who arrives there with the right hand free, and one row of those cuts off everything above it. Seeds 4, 9, 16, 22 and 25 all died that way with the field looking perfect. So the generator now builds every row for BOTH hands, and then walks the wall the way a climber does -- tracking which hand is free -- and opens it wherever the walk cannot reach a goal. TRIED AND DROPPED: blank slabs of featureless rock, to break the field up and halve the geometry. Under this reach rule a blank IS a wall, for exactly the reason above, and the repair that fixes it fills the slab back in. CONNECTIVITY, checked as a graph with no simulation in it: start -> rune -> rune -> rune -> altar is reachable on all four roster seeds and on every seed from 1 to 40. Per roster seed, states (hold, free hand) reachable from the start: Ritual 3790 of 3790, Ladder 3717 of 3718, Serpent 3724 of 3726, Ordeal 3872 of 3872 -- the whole field, either hand, everywhere. Mean DISTINCT holds within reach of a hold: 18.0 to 19.2 (the 27-29 first reported here added the two per-hand lists together and double-counted every hold both hands can take). THE BOT (test/playability.test.js) was rewritten from walking ids to path-finding: it builds the reach graph, runs Dijkstra backwards from each goal, and at every move looks at where its two hands ACTUALLY are and takes the neighbour with the lowest cost onward, so a hand closing on the wrong rock costs it nothing. It drives the real sim with steering input. Per seed: Ritual 157 grabs, Ladder 165, Serpent 162, Ordeal 174; 0 misses, 0 crumbles and 0 falls on all four; 41.4, 43.1, 43.3 and 47.3 s of game time. Every threshold this file has ever had is unchanged. DECOYS (B10) changed shape and got better: a decoy is now a field hold LIFTED OUT of holds[], so it sits exactly where a hold would sit and is worth trying, and route.holds is already the field without them -- which makes 'never the only way up' a thing the connectivity check proves rather than a thing the placement hopes for. Still 8, still never a rune or the altar. intendedHand is now geometric -- which side of the wall's centre line a hold sits on -- because on a field no hold is meant for a particular hand. main.js's debug autopilot still calls it and will pick worse targets for it; nothing in the sim or the generator reads it. TESTS 70 to 77 on the B46 commit, none weakened. Verified: node --test test/ green; a headless-Chrome title screenshot boots clean with the field on the wall; docs/route-field-seed7.svg plots every hold, and shows the field full-width, the rock darkening with height, and the top 1.5 m funnelling into the altar. OPEN, AND IT NEEDS A DECISION: world.js draws one noise-deformed icosahedron per hold at detail 4. At 125 holds that was 0.4 M vertices and 21 MB; at 1900 it is 4.9 M vertices, ~254 MB of buffers and 15 M noise samples at boot. It boots on desktop. It will not boot on Maor's iPhone. world-light needs instancing (a handful of shared blob prototypes) or a detail step that scales with hold count, OR this row's SPREAD constant comes down -- 1.8 would halve the field, 1.2 would third it, and both are still far wider than the old 1.4 m strip. MERGED ONTO B51 (the no-buttons control model) before landing, and the bot was rebuilt on its two primitives -- steerToHold and releaseHand -- so the path-finder does not know how a hand grabs: one move is one gesture, push toward the hold and the hover dwell closes the fingers. Numbers after the merge, on the field: Ritual 156 grabs, Ladder 163, Serpent 159, Ordeal 167; misses 8, 8, 9, 13 (5-8% of grabs, against a threshold of 35%); 0 crumbles and 0 falls on all four; 53.2, 55.8, 53.2 and 57.7 s of game time. Tests 89 and green. One thing the merge turned up that is worth knowing: with the grab automatic and rock everywhere, a web swing now ends almost at once, because the free hand finds a hold within a few frames. test/web.test.js therefore builds a bare wall of its own -- the pendulum has nothing to do with the shape of the cliff -- but the gameplay consequence is real and belongs to whoever owns the web-zip: on a field, swinging is a fraction of a second unless you keep the free hand off the rock. ONE THING LEFT FOR ANOTHER OWNER, and it is user-visible: hud.js keeps its own copy of the four route descriptions in a ROUTE_LINE map (B42), and it still reads '42 jugs, easy rock low down', 'Wanders 0.70 m either side of centre' and '31 crimps, 18 slopers, 68% poor rock up high. 9 decoys.' -- all of it about a line that no longer exists, printed under the pills on the title screen. The roster in route.js already carries the true text in SEEDS[].note and hud.js is handed that list as opts.seeds, so the fix is to render r.note and delete the duplicate. hud-audio owns that file. REVIEW, 2026-09-02, and the corrections it forced. ONE CLAIM IN THIS ROW WAS FALSE: 'a sparser field is not a sparser field, it is a broken one'. The LATTICE cannot be sparser -- its pitch is near the geometric optimum -- but the finished field can be thinned afterwards, and now is: step 6 takes holds out one at a time and keeps only the removals that leave the climb intact and the wall still a field. ROUTE.DENSITY (holds per square metre, null by default) is the knob, with MIN_WAYS_UP 2 and MIN_IN_REACH 8 as the floors it stops at. Seed 7 goes 1965 to 1711, 12.0 to 10.4 holds per square metre, coverage 67.1% to 58.8% of the face inside some grab disc. Across seeds 1-40 the thinned field runs 1516-1751 holds, median 1642, and every one is still connected with its decoys lifted out -- the same guarantee the dense field carries. TWO PLOTS for the owner to choose between: docs/route-field-seed7-dense.svg and docs/route-field-seed7-thin.svg. BOT on each, seed 7: dense 160 grabs, 5 misses, 5 given-up moves, 0 falls, 63.3 s; thin 179 grabs, 7 misses, 0 given-up moves, 0 falls, 61.7 s. HOW FAR IT CAN GO, measured rather than asserted: the floors are what stop it, and they are earning their keep. Relax MIN_WAYS_UP to 1 and seed 7 thins to 1257 -- but the bot then needs 1375 grabs and 322 misses instead of 160 and 5, and does not top out inside its move budget, because half the field has one way up and every mistake becomes a detour. Below about 1250 the local rule saturates anyway: nearly every remaining hold is somebody's last way up. Getting to ~970, as the review measured, needs holds you cannot climb off, which this row's own guarantee forbids and route.test.js asserts against. STEP 5 IS NOT DEAD CODE, and now says so with a number: across seeds 1-40 it adds exactly one hold, on seed 25, and with it disabled seed 25 is unclimbable. It is a one-hold guard that fires once in forty, and on a thinned field it is the thing that keeps the parity repair honest. THE BOT was rebuilt again on top of B51's control model, and three things it got wrong are worth recording because they were all real bugs and not test-fitting: it proposed moving a hand onto the rock it was already holding (the sim refuses that, so the move timed out); it routed through holds at the very rim of the reach circle, which are reachable on paper but often not before the body has finished settling, so it now carries a comfort limit at 0.80 REACH and refuses moves it cannot make; and it burned a hand out, because it left the same hand hanging on a crimp through two three-second failed reaches. It now moves whichever hand is on the poorer rock, alternates, and gives up on a move after 1.5 s rather than 8. ONE THING IS STILL RED, and it is a finding, not a test to fix: 'swing: twelve seconds of pumping past holds never grabs one' (B51's guard) fails on the field. Measured over 24 swings from six heights and four aim angles: with the sticks IDLE the swing survives all 24 for the full twelve seconds, so B51's mid-swing rule works. PUMPED, 24 of 24 end in a grab between 0.55 s and 10.97 s, median 4.45 s. A constant-speed circular sweep, which never dwells anywhere, still ends 7 of 24. Thinning does not fix it: at 1711 holds it is 23 of 24. The cause is that a pump's stick lingers at the ends of its sweep, the rule reads a lingering stick as an aim, and at 59-67% coverage there is rock wherever the arm points. Whoever owns the web-zip has to decide whether a pumped swing is allowed to grab. Test count on this branch after merging main: 116 to 124. |
| B35 | A decoy that crumbled stays gone after Climb again | Claude 2026-09-02, found while building B11 | shipped (v0.3.1) | `restart()` builds a new route from the same seed, so the sim's decoys are whole again, but world.js captured the boot-time fake objects in `fakeParts` and never re-syncs them. Holds already do this through syncRoute/holdById; decoys do not. After a restart every decoy that fell last climb is still invisible and 60 m below, while the sim will happily crumble it again: you can grab a rock that is not there. The v0.3.0 dust lands in the right place only because the same seed puts the decoy in the same spot. Fix is a fakeById sync in syncRoute plus resetting the fakeParts groups. Shipped v0.3.1. `syncRoute` now calls a `syncFakes` that rebuilds `fakeById` from the new route and re-points every `fakeParts` entry at the object carrying its id, then puts the group back: position zeroed, spin undone, visible again, fall and vy reset. It hangs off the same route-identity check the holds already used, so it costs one comparison a frame and runs exactly once per restart. The baked geometry is still right because a restart keeps the seed, and picking a different seed reloads the page. Verified in the browser by driving the real `restart()` the Climb again button calls: two decoys crumbled and fell to -18.21 m, after the restart all seven groups were back at y = 0 and visible, and breaking the same two ids on the NEW route made them fall again -- which is the proof the parts are bound to the new objects and not the old ones. Also measured on the pixels, camera parked a metre off decoy 10000: losing the rock changed 36.2% of the centre of the frame and getting it back changed 34.7%, while the intact frame before and the intact frame after differed by 18% against a 23.9% noise floor measured between two intact frames over the same elapsed time -- i.e. below the noise. Console clean throughout. No test guards it: world.js needs WebGL and `node --test` is the pure-logic suite. |

## The web-zip and the Spider-Man egg

| ID | Item | Source | Status | Notes |
|----|------|--------|--------|-------|
| B14 | Overhaul the web-zip so it feels like the films, not just works | Maor 2026-09-02 | shipped (v0.2.0) | Presentation only; the physics Maor approved is untouched. The line now LASHES as it flies (a decaying sine behind the tip) and snaps straight over a third of a second once it bites. The bite YANKS: 3.4 m/s along the line, 0.35 m of line taken up, a camera jolt and the lens snapping in 7 degrees. Speed streaks tear past the lens while swinging or falling, scaled to real speed. Letting go throws you harder (boost 1.06 to 1.22). Aiming pulls the eye back 0.55 m, opens the lens 14 degrees, and TURNS THE VIEW to the anchor. |
| B15 | The zip has no HUD at all | Claude 2026-09-02 | shipped (v0.2.0) | The right pill now carries the whole state: outlined when the web is ready, pulsing cyan while aiming, solid while the line is out, and a cooldown that drains left to right across it. A reticle marks where the shot will land, with a dotted trajectory from the hand to it. |
| B16 | The zip has no touch path | Claude 2026-09-02 | shipped (v0.2.0) | A WEB pad beside LOOK, same one-thumb press-and-drag: holding it IS the held right grip the sim waits on, the drag is the aim, releasing fires. Only appears once unlocked and dims while cooling. Built on the pattern that fixed B23; UNVERIFIED on a real phone. |
| B17 | Spider-Man hand | Maor 2026-09-02 | shipped (v0.1.0) | No permissively licensed rigged hand exists that downloads without an account, and the two no-login models found were single-mesh photogrammetry sculpts with no skeleton. Built as a shader on the game's own rigged hand instead: radial web across the back, rings around each finger, navy cuff, raised piping that bends the normal and goes matte. Pattern matched to a reference photo Maor sent; palette sampled from a scan, which is gitignored. |
| B18 | The unlock, and making the glove a choice | Maor 2026-09-02 | shipped (v0.1.0) | Typing ARACHNID on the title screen unlocks it for good on that device; letters never double as press-any-key. A customisation panel offers classic, stealth and bare, remembered per device, reachable afterwards from the ✦ button. Changing it rebuilds the hands, which is the honest way to re-skin a skinned mesh. |
| B19 | The 🤘 pose while aiming | Maor 2026-09-02 | shipped (v0.1.0) | Promised in the mockup and missing from the first build. While aiming, the middle and ring fingers fold onto the shooter and the rest stay out, sampled from the model's own grab clip so the curl is the rig's own. |
| B20 | The web-shooter sound | Maor 2026-09-02, "find the original web shooting sound" | shipped (v0.1.0) | The film's thwip is copyrighted audio and was not used. Built instead from a real recorded air swish (Swishes Sound Pack, artisticdude, OpenGameArt, CC0, credited) with a synthesised shooter click and a pressurised spray tail, pitch-varied per shot. 2 KB. |
| B21 | Swing physics on the line | Maor 2026-09-02 | shipped (v0.1.0) | A position-based constraint: integrate, project the body back onto the circle around the anchor, drop the radial velocity. Stable at any frame rate, unlike a stiff spring. The left stick pumps and reels; letting go keeps your velocity so a timed release throws you; catching rock mid-swing returns you to climbing. 8 tests. |
| B48 | The WEB pad's drag aim never reached the sim, so every shot went straight up | Claude 2026-09-02, found while shipping B45; confirmed by Maor | shipped (v0.6.0) | In `read()` the pad's aim vector was written into `out.R` BEFORE the loop that fills both sticks from the pointer, the mouse or the keyboard, and that loop overwrote it every frame (and zeroed it while LOOK was held). On a phone the right stick is untouched while the pad is held, so `out.R` reached the sim as 0, `updateWeb` kept whatever `w.aimX/aimY` already held -- straight up by default -- and every shot went straight up however you dragged. It survived this long because the web tests feed `inp.R` to the sim directly and never go through input.js. FIX: the pad's vector is applied AFTER the loop, so it wins over the right stick and over LOOK for the frames the pad is held, since a thumb on the pad is aiming. The HUD knob is deliberately not fed the aim: it shows the physical stick, which nobody is touching. THE AIM ALSO STEERS THE HAND, and it is marked `active`, so under B45 the arm parks along the line it just shot instead of dropping back to rest. Three reasons: the sim reads one vector, `inp.R`, for both the aim and that hand's steering, so splitting them would need a new Input field and a change to sim.js, which is more than a defect fix; the desktop cursor already behaves exactly this way, being the hand and the aim at once; and a spider hand left pointing at its own anchor is the right read rather than an accident. VERIFIED with the real input.js driving the real sim through a fake pad: a drag up-left now anchors the shot at -4.20, 18.24 and up-right at 4.20, 18.24, while no drag still goes straight up to 0.05, 20.15 -- before the fix all three gave that same straight-up anchor. The whole path still ends in a swing: fire, thumb back on the pad, and the line goes taut at 6.32 m in phase swinging with events release, aim, webshot, release, webhit. TESTS 78 to 79. The new input test drives a pointerdown and a drag on hud.webButton and asserts that `out.R` matches the drag with holdR true, that a finger on the right stick does not outvote the pad, that the HUD knob still reads the stick, and that letting go parks the hand where the aim left it. NOTE ON THE ID: filed first as B47, against a file whose highest row was B46, then renumbered to B48 because main's 8f9a2e8 had already taken B47 for the drag-to-look row and this branch was cut one commit behind main. Ids are never reused. B50 2026-09-02 reverses the second half of this row: the pad's drag is the aim ONLY and travels in its own field, not in `out.R`. Pointing the shot with the same field that steers the right hand killed the right stick for as long as the pad was held and stopped the hand parking, and the claim that the arm stays pointing at its anchor was measurably false (38-60 degrees off through a swing, and a fresh press pinned it straight up). The first half of this row -- the aim reaching the sim at all -- stands, and B50 keeps it by moving the field somewhere main.js cannot drop it. |
| B41 | The whole Spider-Man egg is unreachable on a phone: ARACHNID can only be typed | Maor 2026-09-02, first play on an iPhone 15, "the customisation window doesn't exist and I don't see it" | shipped (v0.5.0) | The only way to unlock is `window.addEventListener('keydown', onTitleKey)` in hud.js, matching the last 8 letters typed on the title screen. An iPhone has no keyboard on that screen and nothing summons one. `refreshCustomBtn()` hides ✦ behind `spiderUnlocked()`, so on a phone the ✦ button never appears, the hand panel does not exist, the glove is never worn and the web-zip -- B14 to B21, the whole of v0.2.0 and most of v0.1.0's easter egg -- is dead code. Maor plays on a phone, which means he has never seen any of it. Needs a touch path to the code: a hidden tap sequence on the sigil, a long-press, or a real text field. Whatever it is must stay a secret worth finding, which is the design question. Shipped v0.5.0, and not by any of the three touch paths that were drawn for it (mockups/unlock-b41.html: a letter ring around the sigil, eight taps, or summoning the keyboard). Maor threw the question out and answered a better one: "just make it so that choosing the Spider-Man hand will automatically activate it." So wearing the glove IS the unlock. `spiderUnlocked()` is now `spiderSkin() !== 'bare'`, the default skin is `bare` so a first climb is still your own hands, and the ✦ panel is no longer hidden behind having the thing the panel gives you. Typing ARACHNID still works and now just sets the skin. Also from the same message: BOTH hands wear the glove, not only the right -- the left model is mirrored so its webbing mirrors with it. The web-zip is still right-handed, because that is the sim's and Maor did not ask to change it. |
| B49 | The blue wrist plugs on the hands | Maor 2026-09-02, after playing v0.5.0, "Get rid of the blue balloons on the hands. I hate it" | shipped (v0.6.0) | The blue balloons were the wrist plugs: one sphere per hand, plugGeo scaled to 59 x 26 x 45 mm and offset 7 mm back along the arm, parented to the hidden cuff group and painted a flat MeshPhysicalMaterial in the glove variant's cuff blue (0x2a4a8c classic, 0x0c1120 stealth). A separate lit object in a different material from the glove, with none of the webbing and no normal map, and it skipped the convertSRGBToLinear that the glove shader applies, so its blue came out brighter and more saturated than the glove's own cuff. THE PLUG WAS CLOSING A HOLE THAT IS NOT THERE. It dates from v0.4.0, where it was a long ellipsoid filling the open-ended CUFF TUBE; B44 hid the cuff, and the plug was then re-described as closing the hand model's open wrist cut. There is no open wrist cut. realistic_hand.glb was parsed directly and every edge counted: all 4554 edges are shared by exactly two triangles, at every weld tolerance from exact float equality down to 0.27 mm, so the mesh has zero boundary edges and zero boundary loops. It is watertight, and it ends in a closed rounded stump about 32 mm across and 19 mm deep at the very end, opening to about 42 by 29 mm a few millimetres in -- which is why a 59 by 45 mm plug stood roughly a centimetre proud of the wrist on every side. Hiding the plug and rendering confirmed the geometry: the hand ends in a clean closed stump, no see-through and no dark void, both at the game camera and from straight up the arm from below. So this is option (a) and the plug is deleted outright rather than restyled. Gone: plugGeo, wristMat, the mesh, its entry in the cuff group and in arms.sides, and with them plainSkin, skinTone and the averageColor helper, which existed only to colour it, plus the SPIDER_VARIANTS import that only wristMat used. Nothing was added, so there is no new material to go wrong and the bare hand needs no special case: the hand ends in its own mesh, so whatever paints the hand paints the end of it. With a glove on, applySpiderSkin's own shader carries the red down into the cuff blue and runs the webbing over the stump; bare, it is the model's own skin and texture. Nothing else in arms.js was touched -- not the camera, the hand scale, the IK, the curl, the tremble, or any constant. VERIFIED: node --test test/ is 70 of 70. arms.js has no unit tests and the full game will not start headless, because the climb begins on a user gesture, so the wrist was rendered through a throwaway harness page that imports src/arms.js alone, fakes a sim state, and frames the shot with camera.js's own constants and framing-dolly rule copied verbatim (EYE_UP 0.30, EYE_OUT 0.40, PULL_MAX 0.14, FRAME_MARGIN 0.92, fov 84 portrait at 390 by 844). At that camera the before shot shows the two blue beans at the bottom of both wrists and the after shot shows both gone with the wrists ending cleanly; wrist close-ups in the classic glove and in bare skin show the same; console clean; and after the change the harness reports the plug as absent. The harness is not committed. WHAT TO CHECK IN A REAL BROWSER, because the harness is not the game: at the close camera, both wrists with both hands gripping, and one hand mid-reach with the other on rock, in bare skin and in both gloves, classic and stealth. Look for any blue at all at the wrist, and for the wrist end reading as a closed hand rather than as a hole or a black cap. The one thing the harness could not reproduce is the game's real lighting, HDRI and bloom on that stump, so if the wrist end reads too dark under the low sun, that is the thing to report -- and the fix would be lighting or the glove shader, never a new object at the wrist. |
| B50 | The web-zip does nothing on a phone | Maor 2026-09-02, after playing v0.5.0, "the web mechanics are really bad, don't work, and require a huge fix - they do nothing" | shipped (v0.6.0) | The owner played v0.5.0 on his iPhone 15 and reported the whole ability as dead. It was worse than dead. Diagnosed first, by driving the real `createInput` through the real sim with pointer events on the pad and nothing else, one gesture at a time. Six faults, in the order a thumb meets them. (1) With both hands on the rock -- the state you are in for almost the whole climb -- holding the pad did literally nothing: `updateWeb` required `!hand.gripping` to start aiming, and the pad, unlike the desktop right button, sends no grip tap to free that hand first. No event, no HUD change, no way to discover the rule. Measured: 1.0 s of holding, `mode` still 'idle', events []. (2) The HUD agreed with the bug: `mark` was only 'ready' when the right hand was already off, so the pad rendered dim and unlit in exactly the situation where a player would press it. (3) A press shorter than WEB_AIM_HOLD (0.16 s) was swallowed in silence. (4) THE ONE THAT MATTERS: the sim cut an ATTACHED line whenever `holdR` went false -- but lifting the thumb is what FIRES, so the thumb is always off by the time the line bites. Measured end to end: shot fires, line flies 11 frames, attaches, phase 'swinging' for a single 1/120 s step, then `!holding` cuts it, SWING_RELEASE_BOOST throws the body, `beginFall` runs, and with the rope gone (B43) that is the whole cliff. Events in one breath: release, webhit, fall, webcut; body fell from y 18.2 to the ground. The web-zip did not do nothing -- it killed you, unless you knew to put the thumb BACK on the pad before the hit and hold it there for the entire swing. Identical on desktop, where `holds.R` is only true while the button is down. (5) While the pad was held it overwrote `out.R` every frame, so the right stick was completely dead (measured (0,1,active) with a finger pushing the stick hard right) and the right hand could never park -- the opposite of what B48 promised. B48's claim that the arm 'stays pointing at its anchor' was false besides: `_stick` is a shoulder-relative offset that rotates with the body, so the arm drifted 38-60 degrees off the line through a swing, and a fresh press reset the pad to (0,1) and pinned the arm straight up. (6) A long line pumped sideways swung the body to x = 7.40, 2.9 m off a cliff that is 9 m wide. FIXES, and the rules chosen. The gesture is now one rule on both devices: hold to aim, let go to fire, TAP to let go of the line. Aiming no longer needs a free hand (`!hand.gripping` dropped from the idle->aiming test): you may aim with both hands on the rock, and both hands come off only when the line actually bites. Chosen over 'the pad only works with the right hand free' because losing a hand now costs the whole cliff, so the ability must not charge you one just to look at where you might shoot. Verified: 1.0 s of aiming with a hard drag moves the right hand 0.0000 m and emits no 'release'. Cutting an attached line is now an explicit edge -- a press-and-lift inside 250 ms on the pad, or a right click -- never `!holdR`. A press-and-HOLD while attached is deliberately not a release, so a thumb resting on the pad cannot sever the line. The pad's drag is now the AIM ONLY, in its own field, reversing the second half of B48: one field cannot mean both the aim and the right hand's steering, and when it did, both came out wrong. The right stick now steers the right hand while the other thumb aims (measured: hand x 0.31 -> 0.72 with the pad held), and the hand parks under B45 again. WEB_AIM_HOLD is skipped for the pad (`web.active`), which is why a short press is no longer swallowed: the pad only ever means 'web', unlike the right grip button it shares a threshold with, so it aims on the first frame and the feedback for a quick tap is the shot itself. No HUD message was needed for that case. PLUMBING, and the trap that nearly repeated B48: main.js builds the sim's input by naming the fields it forwards (L, R, tapL, tapR, holdL, holdR), so a new top-level Input field is dropped before the sim sees it -- exactly B48's bug. main.js is not this branch's to edit, so the gesture rides on `R.web`, the right hand's own control group, forwarded by reference; `Input.web` is the same object under its contract name. The touch test models main.js's copy faithfully so this cannot rot. HUD: the pad now carries the four marks and the cooldown drain itself (web-ready / web-aim / web-out / web-cool plus --cd), not only the right pill, so nothing is lost when the GRIP pills go (B51); 'ready' now means what the sim means. A one-line hint fires once per session the first time the pad is usable, at t > 6 s so the opening message does not eat it. PHYSICS: none of the constants Maor approved in B14/B21 were touched -- WEB_RANGE 6.5, WEB_MIN 1.1, WEB_SPEED 26, WEB_COOLDOWN 3.0, WEB_YANK 3.4, SWING_OMEGA/ZETA, SWING_PUMP, SWING_REEL and SWING_RELEASE_BOOST are all unchanged, and WEB_MIN was measured innocent (a realistic aim anchors 6.50 m out, nowhere near the 1.1 m floor, and no shot in testing ever returned 'webmiss'). Two constants were ADDED, none retuned: SWING_MAX_X 4.2, the same clamp `aimPoint` already applied to the anchor, now applied to the swinging body (before 7.40, after 4.20, across four route seeds); and WEB_AIM_DEAD 0.15 of the pad radius, the drag that commits a press to being a shot. The on-the-circle invariant is now relaxed on frames where the floor or that clamp is holding the body off the line, and to keep that honest the test asserts each flag is telling the truth -- grounded means y is exactly FLOOR, walled means abs(x) is exactly SWING_MAX_X -- and that such frames stay a minority of the swing. VERIFIED: 94 tests, up from 79, none weakened -- and six of the old ones were STRENGTHENED by deleting the `holdR: true` they had to carry through every swing, which was the workaround for the bug. New test/web-touch.test.js drives real pointer events on the pad through the real input and sim: the whole chain with no second touch (aim -> webshot -> webhit -> 'swinging' -> left-stick pump vx 1.71 -> 4.43 -> tap -> webcut thrown at 4.65 m/s), a resting thumb not cutting, aiming not costing a hand, a 17 ms press still firing, the right stick live while the pad is held, the swing staying on the cliff, and the desktop right button doing the same. That last one earned its place immediately: it caught an undeclared `rightDownT` that every other test walked past. NOT VERIFIED: no one has touched a phone. The pad's 62 px hit target, whether a right thumb can tap the pad while a left thumb pumps, and whether 250 ms is the right tap window for a real thumb are all things only the device can answer. REVIEW PASS, same day, five findings, all fixed before shipping. (a) `web.tap` was never consumed, so it stood true across every 120 Hz sub-step of the frame that read it: when the bite landed on a non-final sub-step the next one saw the tap and cut, resurrecting the exact attached-for-one-step fall through the sub-step loop. The sim now consumes `tap` and `cancel` the first time it reads them, and only a line that was already attached BEFORE the step began can be cut, so a tap can neither cut a shot in the air nor the bite it spans. Consuming in the sim rather than main.js is deliberate: the integrator's tap plumbing goes away with the GRIP buttons. Pinned with a test that flies to one sub-step short of the anchor then runs a long frame across the bite, at 2, 4, 8 and 12 sub-steps; with the guards removed it fails with webhit, fall, webcut exactly as the reviewer described. (b) `tapR` is gone from the cut entirely. On a phone a GRIP-R tap mid-swing cut the line AND was swallowed, so the hand never armed; on desktop `tapR` fires on pointerDOWN, so the line went the instant the button touched rather than on the click. A GRIP tap mid-swing is now a grab that reaches the hand, and the flying and attached branches no longer swallow it. (c) THE BRUSH RULE, a manager decision that Maor may want to overrule: with the aim threshold gone, a one-frame brush of the pad fired an unaimed shot straight up, which always bit and took BOTH hands off the wall -- with no rope, a death from a stray touch while reaching for the stick. A press now only becomes an aim once it COMMITS, by being held past the 250 ms tap window or dragged past WEB_AIM_DEAD; until then it is not holdR, so it neither aims nor fires. A brush therefore does nothing. The cost is that an undragged press shows no reticle for its first 250 ms, which is the deliberate trade and the thing to overrule if it feels sluggish on the device. A drag commits instantly, so drag-to-aim is unaffected. WEB_AIM_HOLD is untouched and still governs the desktop right button, which shares its button with the grip. (d) The one-time hint could never fire on Maor's own flow: it sat below `updateWeb`'s cache early-return, and on a plain climb the pad sits at 'ready' from the first frame so the cache key never moves. Evaluated before the cache check now; measured firing at t = 6.02 s on a 900-frame plain climb, where it previously never fired at all. (e) A stolen or cancelled pointer mid-aim FIRED an unaimed shot. It now aborts the aim: mode back to idle, no shot, no cooldown, both hands still on the rock. |

## HUD, camera and onboarding

| ID | Item | Source | Status | Notes |
|----|------|--------|--------|-------|
| B53 | A ground to fall to, and a blackout when you hit it | Maor 2026-09-02, "create a ground that the player will fall to when he lose the grip and when he hit it the screen will go blank like he died" | shipped (v0.6.0) | Direct instruction. Until now the wall simply ran on down into the fog: B24 built the plunge and B43 made it the only outcome, but there was nothing at the bottom of it, and the death read as a red tint rather than a death. THE GROUND, in world.js, one delimited block. Height -0.55 m, and the reason is CFG.FLOOR: 0.75 is the SHOULDER of someone standing at the base (phase 'grounded' pins body.y there), and the eye is body.y + 0.30, so the dirt has to be a body's shoulder height below FLOOR and not at it. At -0.55 the standing eye is 1.60 m above the open terrace and 1.40 m above the scree banked against the rock, which is a person; at FLOOR - 0.75, the other end of the range, the climber would have been staring at his own knees and the start holds at y 1.2 would have stopped reading as something you reach up to. SIZE 30 x 30 m: x in [-15, 15] under the whole 9 m face and past both flanks of the 13 m slab, z from -6 (behind the rock) to +24 (out past the camera), 96 x 96 cells, 0.31 m each. Relief is three octaves of the wall's own noise, about +-0.25 m; the outer ring rolls away 3.4 m into the cloud sea that was already down there, so the terrace reads as a ledge and not as a slab cut out of the air. NO SEAM AND NOTHING SKIRTED: wallZ is untouched, and the wall plane already runs from y -8 to 43.5, so it passes 7.45 m through the ground and out the bottom -- the join is a real intersection. What sells it is scree, 0.12 m of talus banked against the rock over a 1.4 m run, plus a shadowed corner in the vertex colours. Both are deliberately small: they sit exactly where the climber stands, so every centimetre comes off his height, and the first draft's 0.55-over-0.9-m corner made the ground he lands on nearly black. MATERIALS: no new assets. The plane is wallMat cloned -- the same rock_face_03 2k diffuse and normal, the same 3 m tiling, normal scale dropped 1.45 to 0.85 because the ground is read at a grazing angle where 1.45 boils into sparkle -- with its own vertex colours: dust paler and greyer in patches, bare stone keeping the wall's iron red, so the floor does not read as more wall lying down. The twelve boulders are ONE InstancedMesh of ONE B52 hold prototype (the sloper, the family with least chalk on it) laid on its back so the blob's flat front is the weathered top and its stretched back is buried, with per-instance yaw, a small settle tilt, non-uniform scale and a cooler shade. Five are scree in the corner at the foot; the other seven are boulders out on the terrace, kept clear of where the climber lands so a rock cannot end up inside the lens on the last frame. LIGHT AND FOG come free: the same material, the same scene fog and the same key/hemi/env lights that env.update already schedules, so the ground goes through dusk to night exactly as the wall does and takes the same shadows. COST at tier phone, measured before and after by hiding the two objects in the live game: standing at the base +3 draw calls and +30,432 triangles (18,432 ground, 6,000 boulders, 6,000 boulders again in the shadow pass), 58 calls and 351.5k triangles for the whole scene; from 12 m up +1 call and +18,432 triangles, because the boulders leave the shadow pass on the same rule the holds use (world.update drops castShadow above GROUND.y + PROTO.shadowBand) and then leave the frustum; above about 20 m nothing at all. The ground plane itself is never hidden by height, because looking down the drop is the point of it. THE DEATH, in camera.js, hud.js and index.html, all keyed on the sim's `impact` event and not on phase 'fallen' -- they arrive in the same frame, but the cut belongs to the ground arriving. At 0 ms the eye takes the last drop: the sim parks the BODY at CFG.FLOOR, a standing shoulder, and a climber who has just arrived at 26 m/s is not standing, so the eye falls the remaining 1.25 m to 0.35 m above the ground over 120 ms, ease-out, under the roll jolt that was already there. Over the first 80 ms the screen CUTS TO BLACK -- opaque #000 at opacity 1, animated by `deadCut 80ms linear`. It holds 1200 ms, and then "You fell" fades in OVER the black, which stays: the wall never comes back for the death screen. The black is `body.dead-veil::after` at z-index 15 and not the old `#hud.dead::after` at 45, for two reasons: #hud fades out under showEnd and would take its own pseudo-element with it, and z-index 15 sits over the canvas and the HUD and under the overlays, which is exactly what lets the end screen come up on it. `#hud.dead` survives as the state flag every reset path already clears and now hides the HUD outright; `#mute`, `#customBtn` and `#menuBtn` are not in #hud (they float at z-index 40) so they are hidden too until the end screen raises `overlay-up` and takes them back -- without that, three lit circles sat on an otherwise empty screen. GONE: the maroon radial veil, its `deadFlash` keyframes and the message 'The cliff keeps you', which was being drawn on a screen that is black by then. KEPT: `#fallflash` through the fall itself, and the impact cue in audio.js, which was already keyed on the event's own name and so already landed with the cut rather than after it. `grounded` -- letting go with your feet still on the ground -- pushes no `impact` and never reaches phase 'fallen', so none of it can happen to someone who was standing; that was tested, not assumed. world.update also throws a puff of grit out of the dirt on `impact`, from the decoy dust pool, which is what the last frames under the fade show. VERIFIED in a GPU-backed headless Chrome (ANGLE Metal) at 390x844 at 2x and in a live browser, seed 7, tier phone, console clean (window.__ritual.errors.length === 0) at every step. Photographed: the ground from the start looking 55 degrees down (the two-handed limit); the drop from 11.7 m looking down, ground and horizon at the bottom of the frame; the last frame before impact, frozen at body.y 0.96; and the black, in portrait, in landscape 844x390 and at 360x740 -- all three decoded pixel by pixel, every channel of every pixel exactly 0. Sampled every frame through a fall from 12 m: the black class lands on the impact frame and the layer reaches opacity 1 within about 95 ms of it (the 80 ms the CSS is written for, plus a frame of measurement lag), the eye reaches -0.20 in 120 ms, the end screen at +1209 ms. Round trip tested: climbing, letting go at the base (phase 'grounded', no black, eye unmoved), a fall from 12 m, black, end screen on black, Climb again -> a clean climb with the eye back at 1.08 and no black left; and the same again out through the end screen's Menu to the title. node --test test/ passes 116/116. NOT VERIFIED: any of it on a real phone, which is still B27 and B25. Two things noticed and left alone as other rows' work: `debug.fall()` in main.js still sets taps, which stopped letting go of anything in B51, so it has been dead since then (every fall here was driven by pushing both sticks, as a player would); and in the last second of a fall the gaze pitches to nearly straight down, where camera.up is almost parallel to the view and the horizon can land at any angle -- pre-existing, and only visible now that there is something down there to see. |
| B22 | Look around with a hand free, including down into the drop | Maor 2026-09-02 | shipped (v0.1.0) | Only with exactly one hand on the rock, because the free arm is what lets you turn. The reachable arc follows the free arm: hanging off your right you can crane left. 180 degrees across, 62 up, 85 down, and staring down widens the lens for vertigo. SUPERSEDED by B47 2026-09-02: Maor wants looking to be a drag on the screen and to work with both hands on the rock, which retires the free-arm rule this row was built on. |
| B23 | The LOOK button did nothing on iPhone | Maor 2026-09-02 | shipped (v0.1.0) | Holding it only enabled looking; the direction had to come from a second thumb on a stick, so pressing it alone did nothing at all. It is a drag-pad now, one thumb, press and drag. Pressing it with both hands on the rock says why nothing happens instead of feeling broken. SUPERSEDED by B47 2026-09-02: the drag-pad this row built is being replaced by dragging the screen itself, so the LOOK button goes entirely. |
| B24 | Falling animation and a death screen | Maor 2026-09-02 | shipped (v0.1.0) | With the rope spent, losing both grips is the whole cliff: no catch, terminal velocity, the head pitches over, the world turns, the lens widens 22 degrees with speed, the wind roars up with it. The ground lands one heavy hit, the screen blooms red and the end screen reads "You fell". Measured 19.7 m to the ground at 16.8 m/s. Verified in a desktop browser only, which is B25. B53 2026-09-02 gave the drop something to arrive at: there is a ground at y -0.55 now, and the red bloom this row describes is gone -- the screen cuts to black instead. |
| B25 | Verify the fall and death screen on a real phone | Claude 2026-09-02 | proposed | The red bloom and the 22-degree lens widen were tuned on a desktop screen and may be too much on a phone. PREPARED 2026-09-02 -- the numbers, so the phone check is two minutes of looking rather than an investigation. First, a correction to this row's premise: there is no red bloom. `post.js` has no death path at all; its bloom only follows `setNight`. The red is a CSS overlay, `#hud.dead::after` in index.html -- a radial gradient `120% 80% at 50% 60%` running from transparent at 30% to rgba(96,8,8,0.85) at the edge, over a flat rgba(60,4,4,0.35), animated by `deadFlash` 1.5 s ease-out: opacity 0 to 0.92 at 90 ms, settling to 0.42 until the end screen replaces it 2.4 s after the ground. Composited, that is about 32% coverage at the centre of the frame and 73% at the corners at its brightest, settling to 15% and 33%. It DARKENS toward near-black maroon rather than flashing bright, which is the kind direction on an OLED; the risk is not glare but that it hides the ground you just hit. Second, the lens. B24 says 22 degrees and camera.js does say `22 * doom`, but `doom` peaks below 1 and there is a separate `6 * fallBlend` on the same fov line, so the real widening is 6 + 22*doom on top of the 84 degree portrait base: measured 104 degrees from a 10 m fall, 108 from 20 m, 110 (the clamp) from 30 m up. Then the ground snaps it 10 degrees narrower in one frame and it eases back over about a second. On a 6.1 inch screen at 30-40 cm a 24-degree vertical widening is a far larger apparent movement than the same number on a desktop monitor, so this, not the red, is the likely offender. Third, the rest of the plunge: `#fallflash` lays rgba(10,12,24,0.55) over the whole screen for the length of the fall; the world rolls by `sin(tumble*0.9) * 0.5 * doom` radians, plus or minus 19 degrees from 10 m and plus or minus 24 from 20 m, at about 0.5 Hz; shake runs at `doom * 0.8`, up to 2.5 cm of eye translation; and the ground adds a 9-degree roll jolt that decays over about a second. Fall timings straight from the sim (GRAVITY 9.81, FALL_TERMINAL 26, floor at y 0.75): 1.37 s and 13.5 m/s from 10 m, 1.98 s and 19.5 m/s from 20 m, 2.83 s and 26 m/s from the 40 m summit. MAOR'S CHECK, about a minute: open https://maores.github.io/rock-climber-ritual/?seed=7 on the iPhone, climb past 10 m (the big gold number is the height), then let go of both GRIP pills and grab nothing -- nothing catches you now, B43 -- and watch the whole way down plus the two seconds after. Three questions. (1) Is the red too dark or too long, or does it read right? (2) Does the lens widening make you queasy, or does it just read as speed? (3) Is the shake readable as an impact, or is it just blur? TUNING KNOBS, one line each if an answer is 'too much', none of them changed here. Lens widen -- `22 * doom` and `6 * fallBlend` in the fov line of src/camera.js (arms-camera), now 22 and 6 degrees, sane range 10-22 and 3-6. World roll -- the `0.5 * doom` factor in the `rollNow` expression of src/camera.js, now 0.5 rad (28.6 degrees), sane range 0.2-0.5. Fall shake -- `doom * 0.8` in the `totalShake` line of src/camera.js, now 0.8, sane range 0.4-0.8. Impact jolt -- the `-10 * impact` fov term and the `degToRad(9) * impact` roll term in src/camera.js, now 10 and 9 degrees, sane ranges 4-10 and 4-9. Red veil peak and hold -- the `deadFlash` keyframes in index.html (hud-audio), now 0.92 at 6% and 0.42 at 100%, sane range 0.5-0.92 and 0.2-0.42. Red veil colour -- the `#hud.dead::after` background in index.html, now rgba(96,8,8,0.85) at the edge over rgba(60,4,4,0.35), sane range 0.5-0.85 and 0.15-0.35 on those two alphas. Fall darkening -- `#fallflash` background rgba(10,12,24,0.55) in index.html, sane range 0.25-0.55. The row stays proposed: only the phone can answer it. |
| B34 | The LOOK and WEB pads sit on top of the bottom of the two sticks in portrait | Claude 2026-09-02, found during B28 | shipped (v0.3.1) | Measured at 390x844 with an iPhone 14 Pro's insets: the LOOK pad is 80x31 px and sits entirely inside the left stick's 136 px ring, WEB the same over the right. Both are z-index 40 against the HUD's 10, and elementFromPoint inside the overlap returns the pad, so a thumb sliding down the bottom of a stick hands its touch to LOOK. Pre-existing, not caused by v0.3.0. Left alone because B28 was scoped to landscape and the portrait layout was not to be redesigned. The fix is a layout decision (raise the pads, shrink the sticks, or move the pads inboard), which is why it wants its own row. Maor plays on a phone in portrait, so this is the most likely thing to bite him. Shipped v0.3.1, layout A of three put to Maor as a picture (`mockups/pads-b34.html`, real game frame, real control sizes) and picked by him. Each pad is now the top of its own control stack -- LOOK / GRIP / stick on the left, WEB / GRIP / stick on the right -- stacked in the flow inside #hud instead of fixed over it at z-index 40, so an overlap is no longer possible rather than merely avoided. The pad takes the cluster's own width, so the target went from 80 px to 136 px (150 on desktop, 120 on a 360 px phone, 112 in landscape), and WEB sits on the right hand, whose ability it is. Because the cluster is anchored at its bottom edge, GRIP and the stick did not move a pixel and hiding WEB while the egg is locked leaves the two columns level. Verified in the browser at 390x844, 360x780, 844x390 and 1440x900: zero rect intersections between either pad and either stick or grip, every probe around the bottom of both rings now returns the stick where (100, 810) used to return the pad, console clean at all four. On the title screen the pads are display:none under body.overlay-up and #hud is still at opacity 0, so nothing leaks over an overlay. One bug fell out on the way and was fixed with it: the WEB pad's HUD cache key did not include `unlocked`, which flips false to true when the climb starts while nothing else in the key changes -- so on an unlocked device the pad stayed hidden until the first time the right hand came off the rock. |
| B47 | Drag the screen to look around, instead of holding a LOOK button | Maor 2026-09-02, first play on an iPhone 15 | shipped (v0.6.0) | "Instead of a button that you must press to free look, remove the current look-away feature and replace it with the player being able to press the screen itself and drag the view / eyes / POV to where he wants -- of course within the limits of what he can really see. It would cover the cases when the right or left hand is not placed on a rock, AND even when both of them are placed." This retires B22 and B23 rather than extending them: the LOOK pad goes, and with it the reason B34 put a pad at the top of the left column, so that column loses its top row again. Two real design questions inside it. (1) THE FREE-ARM RULE GOES. B22 was built on the idea that the free arm is what lets you turn, so looking needs exactly one hand off; Maor wants it to work with both hands on the rock, which means the arc has to come from the neck rather than the shoulder -- narrower when both hands are planted, wider when an arm is free, and the existing arc (180 across, 62 up, 85 down, following the free arm) becomes the loose case rather than the only one. (2) THE DRAG HAS TO SHARE THE SCREEN WITH THE STICKS AND GRIPS. A drag that starts on a stick or a grip belongs to that control; a drag that starts on bare wall is a look. That is the same z-order problem B34 was, so it must be solved by hit region and not by z-index. Also undecided: whether the view springs back when you let go, or stays where you left it -- which is the same question as B45, and the two should be answered together so the game is consistent about whether letting go undoes what you did. SHIPPED v0.6.0. THE GESTURE: a pointer that comes down on the canvas drags the view; a pointer that comes down on a stick, a grip or the WEB pad belongs to that control and never reaches it. That is a hit region and not a z-index -- the HUD is pointer-events:none with pointer-events:auto children, so "the event reached #gl" is exactly "the finger was not on a control". input.js binds the drag to the surface main.js already hands it as `mouse`, so nothing new had to be plumbed and main.js was not touched. Multi-touch is by pointerId: a thumb on a stick and a thumb dragging the view are two ids and run at once. SENSITIVITY is measured in screens, not pixels: LOOK_SWEEP_X and LOOK_SWEEP_Y are both 2.0, so a drag across the full viewport width sweeps the whole -1..1 horizontal arc and a full-height drag the whole vertical one -- 97.5 px of a 390 px screen is exactly 0.5. THE VIEW STAYS: a drag ACCUMULATES into look.x and look.y, clamped per axis to 1, and lifting the finger changes nothing. look.active now means "a look gesture is in progress" and no longer gates whether looking is allowed at all. THE ARC is what the hands change, and it lives in camera.js: both hands on the rock is the neck alone (neckYaw 60 deg either way, neckUp 40, neckDown 55), one hand free keeps B22's arc following the free arm (yawInward 35 and yawOutward 145, so 180 across, 62 up, 85 down), no hands at all is the same 180 across but symmetric (yawLoose 90). The input is normalised to the arc, so an arc narrowing under you eases the head into it at LOOK.settle 5 instead of snapping to its edge. RE-CENTRE: when a hand takes the rock, or the last hand leaves it, the accumulator eases to zero at LOOK_HOME_RATE 7.5 e-folds per second, which is 95% home in 0.4 s. OWNERSHIP: input.js owns the whole accumulator including that ease and triggers it itself by watching getHands() -- the count of hands on the rock going UP is a grab, going to ZERO is a fall or a swing -- so the gesture has exactly one owner. The split the brief offered as acceptable (input accumulates, camera.js adds its own ease-to-zero correction on the grab event) was rejected after working the arithmetic: with the accumulator clamped at 1 and the camera holding a fixed offset, grabbing while looking hard right leaves the reachable range [-2, 0], so the next drag to the right barely moves the view. DESKTOP: Shift is still the look modifier and now integrates the same accumulator at KEY_LOOK_RATE 1.1 units per second, so Shift plus a stick or the cursor turns the head and the view stays where the keys leave it; a shift-drag or a middle-button drag on the canvas looks; a plain left or right click is still a grip, and a shift-click no longer toggles one. HUD: updateLookBtn, the "Let a hand go to look around" message and hud.lookButton are gone, and both title cards teach the drag instead. index.html: the #look button, its CSS block and its overlay-up mention are gone, so #ctl-l is GRIP then stick and #web is now the only pad in a column (B34 invariant updated in CONTRACTS). TWO BUGS FELL OUT ON THE WAY, both measured, both fixed with it. (1) The turn COMPOUNDED: the rotated gaze was written back into the smoothed look target, so the next frame eased that rotated point only part of the way back and then turned it again -- 60 degrees of yaw came out as 172 on the screen. It rotates a copy now, which is what makes the constants above mean the degrees they say. (2) The yaw was MIRRORED: the eye faces -z, so ADDING lookYaw to the azimuth turned you the wrong way -- dragging right looked left, and the wide arc pointed at the gripping arm instead of the free one, the exact opposite of what B22 has always documented. Subtracting it fixes both at once. VERIFIED in the browser at 390x844 on seed 7, console clean throughout. Hit testing with elementFromPoint: the centre of each stick returns stick-l or stick-r, the bottom and left edges of the left ring return stick-l, the pills return grip-l and grip-r, the WEB pad's own rect (236, 585, 136, 31) returns web while the pixel 8 px above it returns gl, and every bare point tried -- (195,400), (86,560), (195,760), (195,12), (195,838) -- returns gl. The arc was measured as the change in the camera's world yaw across a full-width drag, against a control that drifts 0.1 deg over the same 1.26 s window: both hands on the rock, 53.5 right and 56.5 left against a nominal 60, 33.0 up and 50.0 down against 40 and 55; one hand free, 133.6 toward the free arm and 45.8 toward the gripping one against 145 and 35, mirrored correctly for a free left and a free right hand; no hands, 93.5 and 90.1 against 90. The shortfalls are the gaze point itself drifting a few cm while the body sways and a free hand hovers, not the arc. The re-centre was measured on a real grab: look.x 1.00, then 0.48 at 103 ms, 0.107 at 307 ms, 0.051 at 408 ms and 0.001 at 916 ms, which is e^(-7.5t) to three places. The view staying was measured too: after a drag to look.x 1 and a finger lift, look.x was still 1.000 a second and a half later. Both title cards were measured at 390x844: the touch card's new step 4 leaves TAP TO BEGIN 25 px clear of the credits, the mouse card 9 px. TESTS 70 to 76, none weakened: six new look tests drive input.js headless through the same fake win and now rig -- the sign and magnitude of a drag, a thumb inside a stick starting no look while a second finger does, the accumulator surviving a lift and a second drag adding to it, the clamp at 1, the ease home on a grab and on a fall, the mouse needing Shift or the middle button, and Shift plus a stick turning the head while the hand stays put. NOT VERIFIED: none of it on a real iPhone, and the two-finger case (a stick and a look drag at once) only in the headless test. Noted and left alone: turning hard to the side this close to the wall shows the edge of the cliff mesh and open sky, which is world geometry, not the camera. REVIEW PASS on the same row, after a reviewer measured the shipped build. The two camera bugs above were confirmed independently (the old look ran away past about 40% deflection, up to 3550 deg/s, and it was mirrored), and ten further findings came back; all ten are in. THE BIG ONE: the accumulator was a FRACTION of the arc, so letting a hand go -- which widens the arc from the neck's 120 to the body's 180 -- panned the view by itself, 87 deg in half a second measured. The accumulator now holds absolute DEGREES of yaw and pitch, clamped to the arc that is live, so an arc change moves the limit and not the view; a drag still spans the whole arc across the whole screen, and an arc that narrows under you eases the angle back inside it at 6 e-folds/s. The arc table moved to input.js with it, because that is where the clamp has to happen, and camera.js now takes the degrees and renders them: one copy of the numbers, no way for the two to drift, and the rig keeps only its follow rates. Measured with the real rig driven at a true 60 fps in a harness (the browser pane throttles to about 4 fps, which makes wall-clock camera timings meaningless): a look of 50 deg renders as 50.28 deg of world yaw, and letting a hand go moves the rendered view by 2.98 deg -- against a 3.4 deg control with no look at all, so the accumulator's own contribution is zero and what is left is the gaze re-weighting toward the newly free hand, which is B44's behaviour and not this row's. SECOND: the ease home was eased twice, once by input at 7.5 e-folds/s and again by the rig, so the VIEW took 0.82 s to get 95% home instead of the 0.4 s the ease is written for. `look.homing` now tells the rig to hurry (25 e-folds/s, a 40 ms lag) instead of settling, and the same harness measures 0.47 s to 95% and 92% at 0.40 s, against 0.82 s before. THIRD: the touch title card's fourth step pushed TAP TO BEGIN off a 360x640 phone and clipped the card at 375x667. The step is gone and the line moved onto the tail of the card's hint, where it costs nothing: card height 352 px at 360x640, 297 at 375x667, 297 at 390x844 -- identical to the same card with the sentence deleted, at all three. (The card already overflows a 360x640 screen without it: inner top -34, TAP at 641 in a 640 viewport. Pre-existing, not this row's, and not made worse.) FOURTH: on a desktop a middle-button look dragged the free hand along with it and the hand jumped again when the drag ended, because the stick loop only knew about Shift. The cursor now stops steering for as long as any look drag is live -- its hand vector freezes, the hand parks (B45 `active` false) and picks the same target back up when the drag ends -- while thumb sticks are untouched, because a finger on a stick and a finger on bare wall are two different fingers. THE NITS, all done: a drag during the title's 0.9 s fade used to reach the canvas through a pointer-events:none overlay and start the climb 30 deg sideways, so a look is ignored while the title element is still on the glass; a second finger put down on bare wall now takes the drag over when the first one ends, from where it actually is, instead of being dead until you lift it; pointercancel ends a look and is tested; the vertigo lens is scaled by the live arc's own downward limit, so staring down with both hands on the rock reaches +8.2 deg of fov at the neck's 55 rather than the 42% of the effect it got from dividing by the loose arc's 85; and the middle button preventDefaults so Chrome's autoscroll widget does not appear under the drag. TESTS 76 to 91 on the merged tree, all passing: the look tests are rewritten in degrees and joined by the neck arc, a release not moving the view, a mouse look never towing the hand, pointercancel and the hand-over, and the title-fade guard. NOT VERIFIED: still nothing on a real iPhone, and the composite settle time is measured in a harness at a fixed 60 fps rather than in the live pane, which was throttled to about 4 frames a second for the whole session. |
| B44 | The hands are nowhere near the eyes: nothing like The Climb | Maor 2026-09-02, first play on an iPhone 15, "I wanted (from the start) that the hands will be much closer to the POV of the player, really similar to The Climb 2 perspective, and you didn't do it" | approved | Diagnosed with measurements rather than taste. The eye sat 0.62 m off the wall AND a framing dolly eased it back up to another 0.60 m so that both hands always fit the frame width -- with the hands only 0.50 m apart it had already retreated 0.17 m, putting a hand 0.85 m away. Maor sent a reference frame; two more were pulled from a review of the game. Measured against them: in The Climb a hand is 0.29-0.40 m from the eye and takes 20-29% of the frame width, the arm is never drawn, and only one hand is usually in view. Also measured, and it matters: THE REFERENCE IS 16:9 VR AT ABOUT 100 DEGREES HORIZONTAL, and a phone in portrait is about 45. Two hands 0.50 m apart cannot both sit in a portrait frame up close -- at 0.40 m the frame is 0.33 m wide -- so the framing has to accept one hand at a time, which is what the reference does anyway. Camera distance alone was the wrong variable and the first three attempts at it were rejected: what actually read wrong was the forearm, a tapered olive tube crossing a third of the frame with the hand small at the far end. Maor's call after seeing it stripped back: "I want to see ONLY the hands, no sleeve, no coat, Only hands". |
| B40 | No way back to the menu once a climb starts | Maor 2026-09-02, first play on an iPhone 15 | shipped (v0.6.0) | `hud.showTitle` is called exactly twice in main.js, both at boot -- once on success and once on a boot failure. Nothing calls it again. Once you tap to begin there is no route back to the title, so the route picker, the four seeds and the ✦ panel are all one-way doors: the only way to change your route is to reload the page by hand. The end screen's only button is Climb again, which restarts the same seed. Wants a way out mid-climb (a pause, or a Menu button on the end screen) and a decision on whether quitting mid-climb needs a confirm. Shipped v0.6.0, two ways out. Mid-climb: a third 40x40 button in the top-right cluster, a hamburger beside the ✦ and the mute, at right 108 px against their 60 and 12, three buttons 8 px apart at 242-282, 290-330 and 338-378 in a 390 px window, 35 px clear of the rune dots. It is shown by showHud and hidden by hideHud, so it is up exactly while a climb is and gone under every overlay. Tapping it raises one confirmation ('Leave the climb?' with Stay / Leave) because a mis-tap would throw the climb away, and that confirmation FREEZES the sim while it is up -- a one-hand hang drains 0.20/s and the question must not cost the climb it is asking about. On the end screen: a ghost Menu beside Climb again, no confirmation, the climb is already over. `hud.showTitle` is now the way out as well as the boot call, so it resets its own shell first -- clears endTimer and msgTimer, drops the end screen, removes the dead veil and the falling class, hides the HUD and forgets the cached phase -- which keeps the integrator's side to one function. main.js gained exactly that: `toTitle()` rebuilds the state the way `restart()` does but stops short of `startClimb`, so the state is left in phase 'title' as at boot. Two new public hud methods, `onMenu(cb)` and `onPause(cb)`, both added to CONTRACTS with the two new DOM ids. VERIFIED in the browser at 390x844 by clicking the real buttons: mid-climb at 1.0 s the confirmation froze the sim for 105 rendered frames with 0.000 s of sim time and stamina held at 0.9837, and Stay resumed it (1.000 s in the next second); Leave from 11.69 m with 2 runes lit left phase 'title', height 0.78, 0 runes lit, HUD off, Menu hidden, body.overlay-up on. Death path: fell from 13.73 m, the end screen read 0:17 Time, 13.7 m Highest, 2 of 3 runes, and its Menu returned to the title with the dead veil cleared. The pending-timer case: died at 15.76 m, left through Menu while the 2.4 s end-screen timer was armed, and 4.8 s later the end screen had still not appeared, then the next start climbed clean from 0.78 m. `window.__ritual.errors.length === 0` throughout, and `node --test test/` still 70. FIX PASS after review, same behaviour with five holes closed. (1) The theme kept playing on a title reached this way, because only start() ever called setMusic; toTitle() now calls `audio.setMusic(null)`, and audio.debug() shows it: playing true at 92.6 s while climbing, playing false with url null after Leave, playing true again 1.2 s later once start() re-asked. (2) The Menu button was asking 'Leave the climb?' over a climb that had already ended, and freezing the plunge to ask -- during 'falling' and 'fallen' it now skips the question and goes straight out. Measured: clicked mid-fall at 15.76 m, the confirmation never appeared and the sim was never paused, and the same on the ground with the end timer armed, which then never raised the end screen 3.2 s later. (3) `codeFlash` scheduled the ✦ panel 900 ms out with no handle, so typing the code and tapping to begin inside that window dropped the panel over a live climb; the handle is kept and cleared in hideTitle and resetShell, and the deferred open checks the title is still up. Verified both ways: code then begin at 150 ms leaves the panel shut 1.6 s later, code then wait still opens it, and the ✦ button still opens it mid-climb (deliberately NOT gated on the title -- B41 put the glove behind that button). (4) hud.dispose() was leaking the three #confirm listeners and the code timer; it removes them now. (5) `#confirm .panel` was content-box, so 100% plus 40 px of padding rendered 382 px and overflowed a 360 px phone by 3 px a side -- box-sizing: border-box, now 324 px at 18-342 inside 360. #custom .panel has the same flaw and was left alone, not ours. |
| B42 | The four routes on the title screen do not say what they are | Maor 2026-09-02, first play on an iPhone 15, "I don't understand the 4 choices" | shipped (v0.6.0) | The picker shipped in v0.3.0 as a ROUTE label and four bare pills -- Ritual, Ladder, Serpent, Ordeal. The names were chosen to describe real, measured differences (Ladder is 42 jugs and easy rock low down; Serpent wanders 0.70 m either side of centre; Ordeal is 31 crimps, 18 slopers and 68% poor rock up high) but none of that is on screen, so they read as four arbitrary words. Cheapest honest fix is a line of description per pill, or a difficulty mark; the harder question is whether a first-time player should be offered four routes at all before they have climbed one. Shipped v0.6.0 as the cheap honest fix -- one shared line under the row, not four blocks, and the harder question is untouched. Ritual 'The original line.', Ladder '42 jugs, easy rock low down, poor rock up high.', Serpent 'Wanders 0.70 m either side of centre.', Ordeal '31 crimps, 18 slopers, 68% poor rock up high. 9 decoys.', and an unlisted ?seed= 'An unlisted seed.' -- the numbers B13 measured when it picked them. Inter 11 px in --muted on its own flex line under the four pills, with a 15 px minimum height so a long line swapping for a short one does not shuffle the tap line under it. It shows the route you are on and previews another on pointerover or tab focus, reverting when the pointer leaves the row, so the sentence answers the question BEFORE the tap that reloads the page rather than after it. Keyed by seed inside hud.js, because a seed IS the route and route.js is not hud-audio's to edit. VERIFIED at 360x780: every one of the five lines fits on a single line, the longest measuring 297.4 px of text in a 316 px box, and the four pills still sit on one row. In landscape 844x390 the new line cost 18.5 px on a screen that had 1 px spare, so the landscape block gave it back: sigil margin 5/6 to 2/4, card padding 10/9 to 8/8, card hint 6/6 to 5/5, step margin 4 to 3, tap margin 12 to 8, seeds margin 8 to 6, note 10 px, and #title padding-bottom 30 to 18 -- plus the mouse card's closing hint, a grid item that had been stuck in one column six lines tall, now spans both columns. Net in landscape: the touch card's content sits at top 12.8 with 0 px of overflow (18.6 and 0 before the change) and the mouse card at top 4.2 with 8, where it had been CLIPPED at top -10.1 with 22 px of overflow before anything was added. Console clean at 390x844, 360x780 and 844x390. 2026-09-02, after B46: the four descriptions were a private copy in hud.js and went false the moment the routes became a field (the B46 review caught it). The line now reads route.js's own roster note (SEEDS[].note), so it cannot drift again. |
| B39 | The mouse-button badge on a GRIP pill runs into its own label on a narrow window | Claude 2026-09-02, seen in the B34 verification shots | rejected 2026-09-02 | On a pointer device the grip pills carry an LMB / RMB badge and `.grip.keyed` widens the right padding to 40 px to make room. At the 136 px pill that just works: HOLDING measures 59.7 px and lands at 48.2-107.8 against a badge at 109.1-140.5, 1.3 px of daylight, and `.grip.gripping` then adds a 0.2em text-indent that eats most of it. At the 112 px pill the landscape block gives it, the text box is 48 px and HOLDING and SLIPPING overflow it by about 12 px, so the label runs under the badge and `overflow: hidden` clips the rest. Only bites a non-touch browser in a short or narrow window -- a real phone is touch and gets no badge, a real desktop is 900 px or wider and gets a 150 px pill -- which is why it has waited this long unseen. Fix is either to shorten the labels when keyed or to drop the badge under the landscape block. Shipped v0.6.0, and the row understated it. Re-measured with Inter loaded, reading each label's own Range rect against the badge rect: at the 136 px pill HOLDING already ran 6.54 px UNDER the badge, SLIPPING 8.75 px and ARMED 3.91 px, not the 1.3 px of daylight this row recorded; at the 112 px landscape pill every state collided -- SLIPPING by 32.75 px, HOLDING by 30.54, ARMED by 22.10, GRAB by 8.88, and even the four-letter GRIP by 3.81. So a landscape-only fix would not have been enough. Where the pill carries a badge it now takes a short label table in hud.js (HELD and SLIP for the two long states), 0.1em tracking instead of 0.3/0.2, 22 px of left padding and 34 px of right, and a slimmer badge -- 8 px type, 3 px side padding, 7 px from the edge -- which reserves 26.01 px where the old one took 32.26. The keyed rule has to sit after `.grip.gripping, .grip.slipping`: same specificity, so source order decides the tracking. No pill size changed. HELD rather than the obvious HOLD: the badge beside it says RMB, and HOLD RMB reads as an instruction to hold the button, which is the wrong instruction -- the mouse button is a toggle. VERIFIED by driving the real setPill through hud.update with the badges forced on, then measuring the label's own Range rect against the badge's border-box rect. The METHOD decides the number, and review read it differently, so it was measured a second time: a Range rect ends AFTER the trailing letter-space, which at 11.5 px and 0.1em is 1.15 px of empty track, so a text-box reading is 1.15 px stricter than an ink-only one. Recorded here is the strict text-box reading, because that is the box overflow:hidden clips and the width the label actually occupies; the ink-only figure follows in brackets. Daylight, label right edge to badge left edge, worst state first: at 112 px ARMED 2.97 (ink 4.12), GRAB 8.43 (9.58), HELD 9.43 (10.58), GRIP 11.19 (12.34), SLIP 12.26 (13.41); at 136 px ARMED 12.84 (14.09), GRAB 18.77 (20.02), HELD 21.43 (22.58), GRIP 21.77 (23.02), SLIP 24.26 (25.41); at the 120 px pill of a 360 px window worst 4.84 (6.09); at the 150 px desktop pill worst 19.84 (21.09). Every size clears the 2 px bar on the stricter reading. scrollWidth equals clientWidth in every state at every size, so nothing is clipped any more. Photographed at 844x390 with the left pill slipping and the right holding: SLIP LMB and HELD RMB, both clear of their badges. Superseded by B51 2026-09-02: the GRIP pills are gone, and the badge with them, so the rule this row shipped (`.grip.keyed`) went with the CSS block it lived in. Nothing replaced it — a stick ring has no label to run into. Marked rejected rather than shipped because the fix no longer exists in the build. |
| B55 | The credits box on the end screen is gone | Maor 2026-09-02, "remove the CREDIT window, it's annoying" | shipped (v0.6.0) | Owner played v0.6.0 and wanted the end-screen credits box gone. Removed src/hud.js's showEnd() block that built it -- the `ensure('end-credits', 'div', inner, 'credits')` call and the innerHTML it filled from CREDITS -- and index.html's `.credits`, `.credits h3`, `.credits p`, `.credits b` rules, the `#end .credits` landscape override (max-height 96px, from B28), the `.credits` half of the shared `.foot a, .credits a` selector, and the static `<div class="credits" id="end-credits">` placeholder itself. The CREDITS array in hud.js stays: footHtml() still reads it to build the title screen's #title-foot line, untouched by this row, which still names the hand model and the music with their licences. Attribution for the CC-BY 3.0 hand model and the CC-BY 4.0 music now lives in three places: the title footer, README.md's Credits section, and assets/CREDITS.md plus assets/models/hands/LICENSE.md -- CC-BY only asks for reasonable, visible credit somewhere the work ships, and one always-on screen plus the repo's own docs satisfies that without a second in-game screen. Grepped src/, index.html, review/ and test/ for end-credits and .credits: only the deleted lines matched, nothing else referenced them. Verified: node --test test/ still 116/116, and a headless-Chrome screenshot of the real end screen at 390x844 (window.__ritual.hud.showEnd(state), no full boot needed since showEnd only touches DOM) shows the stats grid closing straight into Climb again / Menu with no dangling gap. |

## Audio

| ID | Item | Source | Status | Notes |
|----|------|--------|--------|-------|
| B26 | A second music track, and ducking under the wind | Claude 2026-09-02 | shipped (v0.3.0) | One 4:24 loop for a climb that runs longer, and it competes with the wind roar during a plunge. Shipped v0.3.0 -- THE DUCKING HALF ONLY. The music sat at a fixed 0.35 and only dipped on the fall and catch cues, so a doomed plunge, which never gets a catch, left it ducked for good. It now follows the wind bed itself: a smoothstep from `wind.level` 0.42 to 1.05, 75% deep at the bottom, ducking in 0.16 s and lifting back over 1.1 s, so it is the wind that pushes the music down and no event list can miss a case. Measured in the browser: climbing at 18 m the music holds 0.35; a roped fall takes it to 0.139 and it is back at 0.335 four seconds after the catch; a doomed plunge saturates the duck at wind 1.87 and 0.088, then restores to 0.349 once the wind dies on the ground. `audio.debug()` now reports `duck`. THE SECOND TRACK WAS NOT DONE: it needs Maor's choice of track and a licence check, so it stays open and wants its own row. |
| B36 | A second music track for the back half of the climb | split out of B26 when the ducking half shipped in v0.3.0 | proposed | One 4:24 loop covers a climb that runs longer, so it repeats. B26 shipped the ducking and deliberately stopped there: a second track needs Maor to choose it and a licence check (CC0 / CC-BY / public domain, credited in assets/CREDITS.md, under 6 MB). Where it would switch (height, night level, or the last rune) is also undecided. |

## Performance and mobile

| ID | Item | Source | Status | Notes |
|----|------|--------|--------|-------|
| B27 | Measure the frame rate on an actual iPhone | Claude 2026-09-02 | proposed | 33 fps was measured in a desktop browser emulating a phone, against a target of 30. That is an inference, not a measurement. PREPARED 2026-09-02 -- everything except the part only the phone can do. The `?fps=1` overlay is live on the deployed build (confirmed by fetching /src/main.js from Pages), but it was written for a desktop screen: 11 px type, under Apple's 11 pt floor, and `right: 12px` with no safe-area allowance. Rewritten in src/main.js, which the integrator owns and is the only file this row touched. The fps number is now its own 22 px line and is coloured -- teal at or above the 30 fps target, amber down to 24, red under the watchdog line -- and the block reads the min fps since the climb started, the number of watchdog step-downs, the tier name and the pixel ratio now. The step count is derived, `(tier.pixelRatio - renderer.getPixelRatio()) / 0.25`, not tracked: the watchdog only ever steps down and only by 0.25, so the distance travelled is the count. Nothing was added to `perf`. Placement was measured at 390x844 @2 rather than guessed: the block sits at top 74 px + safe-area-top, right 12 px + safe-area-right, and is 178 x 141 px, so it clears the rune row (bottom 66 px) at any rune count and the mute and hand buttons (bottom 48 px), and never lands on the height meter -- the first draft, at the old 56 px top, did overlap the runes. Verified end to end in a GPU-backed headless Chrome (ANGLE Metal, Apple M1 Pro) at 390x844 @2: under a 16x CPU throttle the watchdog fired four times, the pixel ratio went 2.00 to 1.00 and the overlay read `18.5 fps / min 15.0  steps 4 / tier phone x1.00 / 390x844`. One known cosmetic: in landscape the block's box overlaps the full-width `#msg` box, though the message text is centred so the glyphs do not collide. MAOR'S CHECK, about a minute: open https://maores.github.io/rock-climber-ritual/?fps=1&seed=7 on the iPhone, tap to start, climb for roughly 60 s and take one fall along the way, then read the block at the top right. PASS is the big number at or above 30 for the whole climb, second line `tier phone`, and `steps 0`. Any `steps` above 0 means the watchdog fired and dropped resolution to keep up -- that is a fail even if the fps then reads 30. `min` is the worst two-second window since the climb started and is the number worth reporting. If the second line says `tier desktop` the phone was not detected: use https://maores.github.io/rock-climber-ritual/?fps=1&tier=phone&seed=7 instead and say so, because that is its own bug. Note the rewritten overlay only reaches Pages once this branch is merged and deployed; until then the live overlay still shows fps, tier and pixel ratio on four small lines, which answers the row, just harder to read. EMULATED BASELINE, not a device measurement: same build, desktop Chrome, GPU-backed headless, 390x844 @2 viewport, `?tier=phone`, 4x CPU throttle, autopilot climbing for 30 s -- 60.0 fps average, min 55.5, pixel ratio held at 2.00, no step-down, 43-44 draw calls, 376k triangles, zero console errors. That is an M1 Pro GPU with a throttled CPU, not an A16, and it says nothing about the iPhone. It is recorded only as proof that the harness, the watchdog and the new readout all work. |
| B52 | Instanced holds: rendering that scales to a field | Claude 2026-09-02, found while building B46 | shipped (v0.6.0) | B46 turns the route from a 125-hold line up a 1.4 m strip into a field of ~1900 holds across the whole 9 m face, and world.js built one noise-displaced icosahedron per hold and merged them all into a single mesh -- a build whose every cost is per hold. MEASURED BEFORE, on this Mac, seed 7, through a harness that boots world.js on its own at the game's own eye (0.40 m off the wall, 0.30 m above the shoulder, 84 degree lens, 390x844 at 2x): at 130 holds the rock was 40,342 vertices, 2.28 MB of buffers, 362,698 triangles a frame and createWorld took 0.43 s; at 1,895 holds (B46's generator, read from its branch at d13b9cc for the measurement only) it was 592,127 vertices, 39.41 MB of hold buffers, 43.05 MB of scene buffers, 2,269,060 triangles and createWorld took 1.72 s, first frame 2.11 s. WHAT SHIPPED: prototype blobs at unit scale -- a grip family (jug, edge, crimp, sloper) times a size tier times three variants, 16 of the 24 possible built for the routes that exist, only ever the ones a route asks for. They are shaped by exactly the code that shaped a hold before, from ranges that are slices of the one range it drew from. Every plain hold is an instance in one THREE.InstancedMesh per prototype, with a per-instance matrix (position, the same seeded +-20 degree lean, the hold's own size, and a +-10% non-uniform scale in x and y only -- never in z, because holdZ depends on it) and a per-instance colour carrying the per-hold shade that used to live in the merged mesh's vertex colours. MEASURED AFTER, same machine, same views: 130 holds -> 14,277 vertices, 0.69 MB, 342,732 triangles, createWorld 0.33 s; 1,895 holds -> 120,555 vertices, 5.39 MB (8.82 MB scene), 2,024,216 triangles at 6 m, createWorld 0.42 s and first frame 0.66 s, 44 draw calls of which about 26 are holds. So at a field: 39.4 MB -> 5.4 MB and 1.72 s -> 0.42 s, against a budget of 60 MB, 40 draws and 3 s. NOTHING WAS DROPPED OR CAPPED. The chalk splats and the contact skirts stay one conformed patch per hold, merged into one mesh each: at a 5x5 and a 4x4 grid they are 61 vertices a hold, so a whole field of them is 5.0 MB and two draw calls, and they are what makes a hold read as growing out of the rock at arm's length. Runes and the summit still get their own per-hold blob; there are four. FRAME COST, which is the part the first pass got wrong: instancing does not remove a single triangle, and the first version quietly added some by giving every jug the fine icosahedron where the merged build gave it only to holds of 0.15 m or over -- 2.46 M triangles at 20 m against 2.27 M before, a 8.5% REGRESSION. The prototypes now carry both size tiers and a hold picks by the merged build's own rule, so the main-pass hold triangles are 945,500 at 1,895 holds and 64,320 at 130 -- identical, hold for hold, to what the old mesh drew. What is actually saved is the DEPTH pass: instances are sorted up the wall, and onBeforeShadow cuts the drawn count to the band within PROTO.shadowBand of the body, restoring it in onAfterShadow. That band is 12 m and not the 4.2 m the first pass used on the reasoning that the shadow camera is a 6.4 m box: the box is 3.2/cos(elevation) tall in world y AND 17 m deep toward a light that climbs 0.34 (sun) to 0.48 (moon) per metre, so it reaches 10.4 m to 11.8 m of wall above the climber. Measured rather than argued: a 4.2 m band loses 5,093 pixels craning up from the start holds and 18,769 at the 1.49 m pull-back, 6.0 loses 2,005 and 8,956, 8.5 loses 411 and 1,484, and 12 loses none anywhere. The saving is low on the wall only, which is where the wall above you is out of the light's reach: 700,000 depth-pass triangles against 945,500 at 6 m, 63 instances of 126 at the start holds, and nothing at all at 20 m, where the band already covers the summit. Frame totals: 2,269,060 -> 2,024,216 at 6 m (-10.8%) and 2,269,554 -> 2,270,210 at 20 m (+0.03%, i.e. parity). THE HEIGHT WINDOW is implemented and provable and, on a 24 m wall, free of charge in both directions: the rows within PROTO.below/above of the body are memmoved to the front of the instance buffers and count is set, on each 0.25 m step of the body. It ships at 24 m either way, which on this route drops nothing -- because tightening it was measured and it pops. LOOK cranes 62 degrees up and 85 degrees down, and the wall then recedes to a vanishing line that is made of distant holds: a 20/16 window is pixel-identical at every normal framing but loses 962 pixels looking down from 20 m and 8,795 looking down from the summit at the 1.49 m pull-back. So the window is what bounds the drawn set on a wall taller than this one, and today it is insurance rather than a saving. Proof that what ships changes nothing: the field rendered from 1.6 m, 8 m, 20 m and the summit, at 0.40 m and at the 1.49 m maximum pull-back, at the game's framing and at both crane limits -- 24 views, 0 differing pixels in every one, at 130 holds and at 1,895. DEPTH: holdZ is untouched, and now it is true. Every prototype's front is normalised to HOLD.front, which used to be the ceiling of a per-hold noise draw rather than its value, so a palm at holdZ floated over the rock it was gripping: 3.86 mm on average and 8.30 mm at worst on the old wall. It is now 0.64 mm mean and 3.71 mm at worst (worst case the other way, palm inside rock, 5.80 mm). PROTO.back 1.06 does the same for the back, stretching whatever back a prototype happens to have so a shared blob is never shallower in the rock than a per-hold one was; the extra PROTO.sink of the first pass is gone, since it moved the rock without moving holdZ and doubled the float. TILT: an instance leans with the wall's local slope, a central difference of wallZ over PROTO.eps 0.10 m, damped by PROTO.tilt 0.55 and clamped to PROTO.tiltMax 0.22 rad. The clamp is on the lean VECTOR now, not on each axis, which is what let 29 of 126 holds lean up to 17.55 degrees past a documented 12.6; the measured maximum is 12.61 degrees and nothing is over. BOOT SAMPLING: placement is five wallZ samples a hold, but that is placement only -- the conformed chalk and skirt patches still sample wallZ per VERTEX, 116,083 calls at 1,895 holds, 32 ms of raw sampling here, and the patch geometry and its merge is the largest slice left of the 0.42 s. DECOYS: they carry no grip at all, so the first pass made every one of them an edge prototype, and the chalk and skirt loop ran over holds only -- a decoy was a shape with no chalk and no contact shadow, which is a decoy you can see coming, and B10 says identical until weighed. A decoy now draws its family from the real holds of its own size and its variant from its own PRNG, and it gets the same chalk splat and skirt as anything else: 1,903 of each at 1,895 holds and 8 decoys, 137 at 130 and 7. What it leaves behind when it goes is that chalk and that shadow, a socket in the wall, photographed. syncRoute now warns once if a route ever comes back with a different shape, because the rock is baked at boot and a mismatch would leave the old wall standing with no clue why. VERIFIED, besides the pixel diffs: the real game at seed 7 through an iframe probe -- 16 instanced meshes, 126 instances, 137 chalk patches, 63 of 126 instances in the depth pass at the start holds, a decoy crumbling with its 18 grit particles and two of them falling, all seven decoy groups home and visible after Climb again (B35 holds), three runes lit at 20 m, console clean, 0 errors. node --test test/ passes 116/116. NOT VERIFIED: any of it on a real phone, which is still B27; and the 1900-hold field is not on this branch, so what ships here is the renderer that can carry it, not the route. WHAT CHANGED IN THE LOOK, honestly: sixteen silhouettes instead of one per hold, so a shape now repeats somewhere on the wall (rotation, the scale jitter and the per-hold shade hide it at climbing distance, and the wall's own texture is per-hold); every hold now protrudes to the same fraction of its size, because the front is normalised; holds lean with the rock instead of all facing dead front; chalk brightness varies with a hold's shade, because an instance colour multiplies the whole blob where the old vertex colours pinned chalk at a fixed white; and the per-hold random draw moved from the shared stream to a PRNG seeded by the hold's id, so the small random details on the 125-hold wall are re-rolled rather than reproduced. MERGED WITH B53, which was built on the first pass of this row: its ground boulders are twelve instances of one hold prototype, and they reached for it as protos[FAMILY_OF.sloper * PROTO.variants] -- an array that the review pass replaced with prototypes built on demand and keyed by (family, size tier, variant). They now ask protoFor(protoKey(FAMILY_OF.sloper, 0)), which is the same sloper blob in the same small tier: twelve instances, one draw call, 500 triangles apiece, and the +30,432 triangles B53 measured standing at the base still hold. The other place the two rows meet is the boulders' shadow cut, boulders.castShadow = body.y < GROUND.y + PROTO.shadowBand, which is now 12 m rather than 4.2 -- and 12 is the number that makes it right, because the shadow volume really does reach that far above the climber. |
| B28 | Landscape pass on a phone | Claude 2026-09-02 | shipped (v0.3.0) | Portrait is designed. Landscape is only known not to be broken. Shipped v0.3.0. Measured at 844x390 and 926x428 with an iPhone 14 Pro's landscape insets injected (59 px either side, 21 px bottom) rather than guessed. Three real defects, all fixed. (1) The end screen was 625 px of content in a 390 px window with `overflow: hidden`: the title was clipped off the top and CLIMB AGAIN sat 56 px below the bottom edge, unreachable after every fall and every summit in landscape. It now fits (no sigil, 26 px title, tighter stats, credits capped at 96 px and scrollable) and the overlay scrolls rather than clips if a shorter phone still runs out of room. (2) The LOOK and WEB pads are fixed above everything at z-index 40 and floated over the title's tap line in landscape and over the end screen's credits in both orientations; they now hide whenever a full-screen overlay is up. (3) The hand panel pushed its own bottom border off the screen and put CLIMB under the home indicator; its swatches are capped at 74 px in landscape. Also: overlays now respect the left and right insets, and the landscape block was moved after the min-width:900 block, so a 926-wide phone gets the landscape controls (112 px sticks, 28 px meter) instead of the desktop ones it was wide enough to inherit. Portrait and desktop 1440x900 are unchanged and the console is clean at every size. Out of this row's scope and still broken: in PORTRAIT at 390 wide the LOOK pad sits entirely on top of the bottom of the left stick ring (80x31 px) and, being z-index 40, takes the touch there; WEB does the same to the right stick. |

## Infrastructure

| ID | Item | Source | Status | Notes |
|----|------|--------|--------|-------|
| B29 | Deploy on GitHub Pages through Actions | Maor 2026-09-02 | shipped (v0.1.0) | Public repo Maores/rock-climber-ritual. The workflow runs the tests first and publishes only if they pass. The Crytek reference stills, the run's evidence screenshots and the Spider-Man scans are deliberately excluded and verified 404 on the live site. |
| B30 | Critic scoring of the build against the frozen rubric | orchestrated run 2026-09-02 | parked | The run was stopped at Maor's usage guard before any critic scored it, so the 7-criterion rubric was never applied. It is the only objective read on how the game looks. Parked on cost, not on merit. |
| B31 | Endless mode after the summit | design interview 2026-09-01 | parked | Maor chose one crafted route with an ending. Recorded so the option is not lost. |
| B32 | Feet and legs | design interview 2026-09-01 | rejected 2026-09-01 | Maor chose arms only. The two-hand mechanic does not need them and they cost animation work. |
| B33 | Ship a rigged Spider-Man hand from a downloaded model | Maor 2026-09-02 | rejected 2026-09-02 | Sketchfab gates downloads behind a login. The two models that download without one are single-mesh photogrammetry sculpts with no skeleton and a scrambled UV atlas. The shader cover in B17 is better than anything available, and no Marvel-derived geometry ships. |
| B37 | CONTRACTS.md no longer matches the render-domain signatures | Claude 2026-09-02, found during B11 | shipped (v0.4.0) | The contract says `world.update(dt, state, camera)`, `hud.update(state)`, `rig.update(dt, state, wallZ)`. The code has `hud.update(state, events)`, `rig.update(dt, state, wallZ, events, lookIn, aim)` and, since v0.3.0, `world.update(dt, state, camera, events)` so the decoy dust can be fired by the crumble event. The events channel is a real part of the frame loop now and the contract should say so, or the drift keeps growing quietly. Shipped v0.4.0, and it was worse than the row thought. Fixed the three signatures it named (`world.update`, `hud.update`, `rig.update` all take `events`) and, while reading the code to check them, five more kinds of drift: the state schema was missing `web` and `route.fakes` and still listed `ropeAnchor` and `fallCount`; the phase union knew nothing of `swinging`, `fallen` or `grounded`; the event list named ten types when the code pushes eighteen, so the contract now groups them and says outright that consumers must ignore types they do not know; the ownership table had no home for webLine.js, webFx.js or spiderHand.js; and the required-DOM-id list was missing eleven ids including the two pads. Added the frame loop as it really runs, the real `window.__ritual` surface with its getters, and the B34 control-layout invariant so nobody re-floats the pads over the sticks. The header no longer claims to be frozen for a run that ended: it says to change it in the same commit as the code. |
| B38 | README does not mention the route picker or ?seed= | Claude 2026-09-02, after B13 | shipped (v0.4.0) | v0.3.0 added four routes and a `?seed=` parameter, documented only in the backlog and in code comments; the README still describes one climb. One paragraph under Play would cover it. Left out of the v0.3.0 commit to keep that change inside the four rows it was scoped to. Shipped v0.4.0 as a paragraph under Play: what the four names actually are as measured differences rather than flavour, that a pick reloads with `?seed=` so a link opens the same climb, and that any seed generates under the same rules. Went in with B43, which had to rewrite the README's opening line anyway -- it promised that the rope catches every fall. |

